package com.infinitecanvas.backend.service.modelruntime;

/** A selectable vendor request protocol exposed by a runtime adapter. */
public record ModelProtocol(String id, String name, String description) {}
