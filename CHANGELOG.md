# 更新日志

## Unreleased

- 后端存储从 SQLite 迁移到本地 MySQL：新增 `mysql-connector-j` 驱动，数据源与方言切换为 MySQL（默认 `127.0.0.1:3306/flowcanvas`，`DB_HOST` / `DB_PORT` / `DB_NAME` / `DB_USER` / `DB_PASSWORD` 环境变量可覆盖，连接自动建库）；画布项目 JSON、生成响应体等大字段由 TEXT/BLOB 升级为 LONGTEXT/LONGBLOB，规避 MySQL 64KB 单列上限。
- 画布外观新增「连接线样式」（曲线 / 直角 / 直线）：正式连线、节点拖拽更新和临时拉线统一使用当前样式，选择按画布项目持久化并纳入撤销 / 重做。
- 连线悬停新增剪刀删除：鼠标悬停正式连线时在路径中点显示小剪刀按钮，点击直接删除该连线，原有右键菜单与 Delete 删除保留；修复按钮显示在鼠标正下方导致连线立即离开、剪刀闪烁的问题，按钮现在稍微偏离连线并对齐路径中点。
- 新建 / Agent 节点自动避让：工具栏与画布创建入口、连线落空创建、脚本 / 剪贴板文本以及 Agent 未指定坐标的新节点统一通过空间索引寻找空闲位置，重叠时向右逐列、向下换行；Agent 批量新增节点仍按现有节点右侧依次排列，显式指定坐标保持原位置。
- 画布新增「注释」节点：独立便签类型（默认 320×200，标题 + 可编辑正文），创建菜单可添加，双击 / 悬停菜单编辑，单击仅选中不打开生成面板；支持缩放、删除、搜索、材质面板和工作流模板，远景模式下由 Leafer 直接绘制正文。
- 多选批量下载：画布多选工具条新增「下载选中」，把选中节点按内容打包为 zip（媒体实际文件、文本/注释正文、其余 JSON），复用现有导出链路并完善后端媒体下载。
- 节点管理面板：画布左侧「画布」页签更名为「节点管理」，保留类型筛选/搜索/定位/预览/多选导出，新增逐节点显示/隐藏、隐藏计数；隐藏节点时同步隐藏相关连线与小地图显示，节点与引用关系仍保存在项目中。
- 快捷键面板升级：新增按功能/组合/说明实时搜索，补齐全选、复制、粘贴、取消等实际快捷键，并加入键位冲突检测（重复键位标红列出涉及功能，无冲突显示正常状态）。
- 画布输入偏好：外观面板新增「滚轮行为」（滚轮缩放 / Figma 平移）与「缩放方向」（常规 / 反向），普通滚轮、触控板双指与 Ctrl/Cmd+滚轮按所选模式处理，设置按画布项目持久化。
- 音频本地工具：音频节点悬停菜单新增「音频编辑」，支持裁剪入点/出点与 0.25×–4× 变速，经 Web Audio 离线渲染导出 WAV 新节点并自动连线（不消耗模型或外部服务）。
- 画布新增「白板」节点：支持画笔、矩形、橡皮、油漆桶、颜色/笔刷、背景图、导出 PNG 与清空，绘制数据随项目 JSON 持久化，单击仅选中不打开生成面板。
- 画布新增「网页预览 / 引用卡」节点：支持 URL 输入并在画布 iframe 预览、切换为标题/摘要/缩略图引用卡，以及在新标签页打开，数据随项目 JSON 持久化。
- 画布新增「拼图」节点：连接图片节点后按宫格合成整图并生成连线的新图片节点，复用本地拼接与账号媒体上传链路。
- 视频节点悬停菜单新增「倒放」：浏览器逐帧倒退重录为无声反向视频并生成连线的新视频节点（本地处理，不消耗模型）。
- 视频节点悬停菜单新增「提取音轨」：浏览器解码视频音频流并导出 WAV 独立音频节点，原视频保留，实现本地音画分离。
- 节点管理面板支持左右停靠：顶部页签旁新增方向切换按钮，拖拽热区、边框与动画自动跟随，位置作为本地 UI 偏好持久化。
- 视频节点悬停菜单新增「关键帧」：浏览器等距抽取最多 6 帧并生成 2 列、自动连线的图片节点（本地处理，不调用识图模型）。
- 多选视频节点后新增「同步预览」：弹窗内多视频同时播放/暂停、重置与跳转，不改变画布数据。
- 画布版本快照：顶栏新增「版本历史」，可保存/恢复/删除最多 5 份项目快照，数据随账号项目持久化。
- 画布主工具栏新增底部/左侧/右侧停靠：左右停靠自动切换纵向布局，素材与外观弹层跟随方向，位置随项目持久化并纳入版本快照。
- 修复画布节点拖拽后 Composer 自动弹出的问题：节点 `pointerdown` 只负责选中，确认点击后才打开 Composer，移动结束仅保留悬停菜单栏；Composer 的 `@` 图片引用改为内嵌缩略图 chip（点击可放大预览），生成时仍序列化为引用标签；图片 / 视频 / 文本 / 音频 / ComfyUI / 生成配置 Composer 的引用区新增「+」方框，点击后进入画布素材节点选择模式，点击素材节点即可建立引用连线，Esc 或顶部提示按钮完成后重新打开 Composer（保留 360 场景三击预览）。
- 画布创作 Agent 新增「任务规划」模式（对齐 VOZEB Agent Run）：一次规划输出创作计划（创作简报 + 视觉方向 + 带依赖关系的产物清单），确认后编译为确定性布局的画布节点并连线，提示词注入简报与视觉方向约束保证整套一致性；执行在服务端进行（新增 `agent_runs` 表与 `/api/agent-runs` 接口）：后端按依赖拓扑派发（并发 2）、经模型代理自调用完成生成、产物落盘用户文件，**页面关闭后任务继续执行**，进程重启后视频任务按上游 id 续取；支持暂停/继续/取消/单任务重试，前端轮询状态并把结果回填画布节点。原「对话操作」ReAct 模式保留不变。
- 新增统一创作 Agent 页（/create）：文字问答、图片、视频、音频在同一对话完成，智能模式走流式工具调用（generate_image/video/audio 自动执行），也可切换 AI 绘图/视频/音频直接模式；支持参考图上传/粘贴（图生图/首帧参考）、会话新建/删除与账号级持久化（generation-logs 新增 chat 类型），生成产物转存账号文件并按 storageKey 重签名展示。修复原生 button 文字色工具类被 antd 未分层 reset 覆盖的问题（新增 .btn-solid-primary）。
- 移除独立的生图工作台（/image）、视频创作台（/video）、ComfyUI（/comfyui）页面与路由：这些能力由画布节点承载，不再单列入口；侧边栏「创作」组整组移除，首页创作入口卡只保留 画布/提示词/素材；共享设置面板组件仍由画布 Composer 复用。
- 新增独立登录/注册页（/login、/register，VOZEB 风格双栏卡片，复用现有后端认证接口）；顶栏新增账号头像菜单（账号设置、退出登录），未登录显示登录入口；工作区门禁「登录账号」改为跳转登录页。
- 非画布页面 UI 对齐 VOZEB-PRO 工作台：顶部横向导航改为左侧分组侧边栏外壳（创作/项目/资产分组 + 底部帮助与管理后台入口，顶栏显示当前页名称），画布编辑器保持全屏不渲染外壳；首页改为徽标胶囊 + 渐变强调大标题 + 中央创作入口卡（建议胶囊 + 各工具快捷入口）；画布库/提示词库/我的素材页头统一为「小字标签 + 大标题 + 描述 + 右侧操作」模式，生图/视频工作台面板与工作区门禁卡片圆角阴影统一；导航数据重构为 `navigationGroups` 分组结构，旧 `AppTopNav` 移除。
- 画布连线端口交互修正：拉线时不再把所有节点的「+」端口圆球全部点亮，只显示当前吸附目标节点对应侧的圆球；修复端口磁吸会被吸进节点内部的问题，磁吸跟随改为只允许向节点外侧移动（最多贴到节点边缘，按缩放动态计算内移上限）。
- 脚本节点分镜表支持幕/场/镜结构化识别：本地解析器可直接识别剧本正文中明确写出的分镜表（「第N幕/集」标题、「场 N」场景行、「SH/SC/镜 N」镜头编号），保留幕归属、场标题、景别、运镜、台词且不截断镜头数；工作台新增「按正文重拆」按钮（本地解析不消耗模型），分镜表按幕分组、幕内显示场标题行，幕名称匹配增加容错；导出与分镜表同源、按幕分列布局，重复导出/逐镜重复生成会替换旧节点不再叠加；AI 拆解提示词要求严格沿用原分镜表逐镜转换，剧本输入上限放宽到 12000 字。
- 修复画布缩小到 50% 以下（远景 overview 模式）时节点连线端口（圆球）不渲染、无法拉线的问题：端口圆球此前在 overview 模式被隐藏，只能放大到 50% 以上才能连线；改为 overview 模式同样渲染端口，配合磁吸区 CSS 反向缩放补偿，任意缩放级别都可 hover 显示圆球并直接拉线。
- 内置 Agent 与生成链路新增影视制作提示词：新增 8 位经典导演风格内置技能（斯皮尔伯格 / 库布里克 / 王家卫 / 诺兰 / 张艺谋 / 侯孝贤 / 韦斯·安德森 / 维伦纽瓦），在线与本地 Agent 选择器均可选用，选中后按导演镜头语言/光线色彩/运镜构图约束组织生成提示词；在线 Agent 系统提示词与本地 Agent BASE_PROMPT 新增影视制作规范（分镜按资产→连续分镜组织、可拍画面描述、景别/运镜术语、同场戏不跳戏、提示词忠于分镜内容、沿用既有角色/场景设定）；脚本节点 AI 拆解与视频解析提示词同步增强连贯性与景别节奏规范。


- 画布 Agent 升级：移除 Codex 线程管理（不再维护线程/工作空间，每次对话使用一次性线程，画布状态即上下文），本地 Agent 面板去掉「历史」Tab 与线程相关 UI；Agent 补齐 ComfyUI 工作流适配——新增 3 个工具（`canvas_comfyui_list_workflows` 列出可用工作流与默认工作流、`canvas_comfyui_get_workflow` 读取字段定义、`canvas_comfyui_set_workflow` 为节点设置工作流与字段值），提示词内置工作流判断规则（按任务匹配 capability，匹配不到用默认工作流），ComfyUI 节点新增 `comfyWorkflowValues` 字段存储 Agent 写入的工作流参数。

- 进入画布前预加载媒体内容：画布列表页点击画布卡片时先显示全屏加载动画（旋转光环 + 画布标题 + 媒体数量），后台预加载该画布的图片 / 视频 / 音频（data:image 图片转存、storageKey 签名 URL 与本地 blob URL 预热），媒体加载完成后再进入画布；预加载带 12 秒超时兜底，失败不阻塞进入。`uploadImage` 增加 dataUrl 内存缓存，画布页恢复时命中缓存避免重复上传。

- 修复时间轴弹窗拖动进度条触发「Maximum update depth exceeded」：出入点裁剪滑块 onChange 直接用 `updateClipRange` 回写，浮点误差（如 2.3499999999999996）经 roundTime 对齐后与 Slider value 不一致，导致 antd 受控校正反复触发 setState 死循环；改为仅在结果实际变化时更新（changed 检测返回原引用）。播放头滑块与 `updatePlayhead` 增加 0.001 容差比较，浮点微小抖动不再触发 setState。

- 脚本节点 AI 拆解按「幕」组织：新增幕/集结构识别（每幕标题/幕名/梗概/时长，如「第二幕·解读与分裂 · 约 45 分钟」），分镜按幕顺序排列并标注所属幕；分镜表按幕分组显示（幕标题行 + 该幕分镜），导出文本带上幕信息。修复长剧本拆解丢失幕的问题：移除分镜 24 条硬截断上限（此前第二幕等靠后内容会被静默丢弃），并提高剧本输入上限到 6000 字。

- 修复脚本工作台导出的 ComfyUI 节点提示词错误写入节点级 `prompt`：导出（分镜/资产）的 ComfyUI 节点现在只把提示词写入 `composerContent`，Composer 编辑写入 `composerContent`（不再写 `prompt`），重试/恢复生成优先读 `composerContent`；节点缩小（<50%）时 Leafer 只显示标题不再显示提示词，避免遮挡画布。用户修改 Composer 提示词即可生效，不再被节点旧提示词覆盖。

- 脚本节点（canvasTool="script"）明确不参与上游文本引用：脚本正文是剧本工作台内容，即使链接到下游节点也不作为参考文本被关联（Composer 引用、提示词拼接、标签收集均排除脚本节点）；普通文本节点行为不变。

- 修复图片节点 Composer 引用多张上游参考图时只发送一张：提示词标签匹配正则要求标签前必须是空格/标点（`(^|[^\p{L}\p{N}_【])`），当标签紧跟中文词（如「参考图片1」）时无法识别，导致该参考图被丢弃；放宽为任意字符前均可匹配（`(^|.)`），后缀仍排除数字字母避免「图片1」误匹配「图片10」。

- 修复画布缩小后节点连线端口（圆球）难以命中：端口磁吸区原为画布坐标固定值，缩放容器 scale(k) 后屏幕尺寸随 k 缩小，缩小画布时鼠标很难碰到端口、圆球小到看不见，必须放大才能连线；改用 CSS 变量反向补偿（`transform: scale(calc(1 / var(--canvas-k)))`），CSS 变量随缩放实时更新、无需节点重渲染，磁吸区（64px）与圆球（24px）在屏幕上保持恒定大小；hover 显示挂在磁吸区上，任意缩放下经过 64px 恒定区域即出现圆球，尺寸适中避免相邻节点靠近时互相误触。
- 移除远景缩放（zoom < 0.5）时节点上方的名称/类型标签（含 overview 标签与普通标题徽标），避免缩小画布后标签按比例放大遮挡画布内容；相关常量与全局 CSS 一并清理。

- 修复在线 Agent 使用 DeepSeek 等思考模式模型时多轮对话报错「The `reasoning_content` in the thinking mode must be passed back to the API」：流式 / 非流式解析现在收集 `reasoning_content`，assistant 与 function_call 消息回传时带上该字段；工具循环、跨轮历史与「批准工具后继续」链路均透传思考内容。

- 修复画布 Agent 工具执行后继续对话报错「An assistant message with 'tool_calls' must be followed by tool messages responding to each 'tool_call_id'」：发送给模型前统一清洗工具消息，连续 function_call 合并为单一 tool_calls 块、孤立的调用/结果与空/重复 ID 自动过滤，流式解析同步丢弃空 tool_calls sentinel，避免严格网关因消息配对不完整返回 400。

- 修复视频生成「下载环节超时」问题：Junli 等上游 CDN 下载速度慢（约 15KB/s，15 秒 720p 视频需 8-10 分钟），此前后端用 `ofByteArray` 等全部下载完才返回响应，前端 60 秒收不到任何字节即报「请求超时」（任务实际已生成成功）。现改为流式透传：GenericOpenAiAdapter / JunliOpenAiAdapter 的媒体下载端点（/content 等）响应头立即返回、body 边收边发；前端下载超时放宽到 10 分钟，节点在下载阶段显示「视频已生成，正在下载」提示，下载失败时保留任务 ID 供刷新后恢复。

- 管理后台新增「请求日志」：新增 `model_request_logs` 表记录经模型代理发出的每一次上游请求（创建 / 轮询 / 下载，含耗时、状态码、脱敏错误原因、对应任务 key），`GET /api/admin/model-request-logs` 按模型 / 状态码 / 仅错误筛选分页查询，用于排查生成失败与超时；日志保留 7 天，写入失败不影响生成链路。

- 修复图片工具弹窗（切分/裁剪/蒙版编辑/高清放大/多角度/扩图/打光）图片不显示：画布保存后图片节点 `content` 存的是带 12 小时签名 TTL 的 URL，弹窗直接使用该旧 URL 会因签名过期而加载失败；改为打开弹窗前按 `storageKey` 重新签名解析（与画布节点渲染、视频剪辑入口一致）。
- 修复「从生成历史选择」弹窗图片/视频卡片不渲染内容：此前只显示占位图标，现在图片按 storageKey 重新签名展示缩略图、视频展示首帧预览。

- 修复画布图片节点「裁剪」弹窗两处交互错误：锁定比例时裁剪框被强制变成正方形并可能被 clamp 成整图（16:9 等非正方形图片上拖动高度瞬间拉满、位置跳变），改为按拖动前的像素宽高比保持比例、以主导方向联动缩放并在边界内收缩；拖动 e/s 手柄越界时不再回退 x/y 导致裁剪框整体跳动，改为限制尺寸并保持对边固定（w/n 手柄保持右/下边缘不动）。裁剪几何计算抽到 `canvas-crop-geometry.ts` 纯函数并补充单测。

- 修复画布 `@` 引用上游文本时方向键下键无法移动候选的问题：ComfyUI 节点 Composer 等文本字段输入 `@` 弹出候选后，ArrowDown/ArrowUp/Enter/Escape 快捷键处理补上 `stopPropagation`，避免事件冒泡到 Leafer 画布引擎被当作移动选中节点而卸载整个 Composer 面板；`@` 引用匹配放宽为不强制要求行首或空白（字段带默认内容时词后输入 `@` 也能引用），快捷键判断兼容 `event.code` 并跳过中文输入法 composition 期间按键。
- 优化画布连线：正式连线与临时拉线统一为连续冷银灰底轨；正式连线仅在鼠标悬停时叠加蓝色流光，普通、选中和关联状态保持静态，临时拉线持续显示流光；拖到空白处打开“引用该节点生成”菜单后，点击菜单外、其他节点或画布空白处会同时收起菜单并清除悬空线，Esc 也可取消。
- 修复节点连接圆球只能在节点已选中时触发的问题：未选中节点的鼠标经过同样会激活 48px 磁吸区、显示圆球并允许直接拉线。
- 修复本地参考图提交视频模型时提示未配置 `PUBLIC_BASE_URL`：本机后端公网地址设为 `https://gycmservice.nas.cpolar.cn`；前端开发服务器的 `/api` 代理新增 `VITE_DEV_PROXY_TARGET`，可在旧后端无法停止时切换到最新后端实例。
- 修复画布 `@` 引用上游文本时最终提示词重复出现 `【文本1】` 的问题：引用标记从正文位置移除，上游正文只追加一次带编号的上下文块；节点连线改为从外侧「+」端口球心发出，端口支持有限半径磁吸跟随并直接拉线，选中节点只显示四角缩放点；同时移除火山方舟 Agent Plan / Seedance 专用协议、默认能力模板与前端专用任务分流。

- 画布工作流模板升级为账号级持久化（对齐 LibTV「创建工作流 → 我的工具箱」）：新增 `canvas_templates` 表与 `GET/POST/DELETE /api/canvas-templates` 接口（按账号隔离，节点组与连线以 JSON 存储），画布工具箱的模板列表 / 保存 / 删除全部走账号 API，跨画布、跨设备可用；原项目级 `workflowTemplates` 字段移除。

- 画布视频合成节点补齐时间轴完整交互（对齐 LibTV 视频合成）：新增「时间轴编辑」弹窗，按连线顺序载入视频片段与独立音频轨，支持片段拖拽排序、入点 / 出点裁剪（双滑杆或 I/O 快捷键，最短 0.2s）、音频轨逐片段静音（预览与导出按静音状态混音）、空格播放 / 暂停、Delete 删除片段、←/→ 移动播放头、↑/↓（Shift 精调）调整片段长度；合成导出沿用 canvas + Web Audio 混音 + MediaRecorder 方案，生成带连线的新视频节点。

- 画布视频节点新增视频工具集入口（对齐 LibTV 视频工具）：「解析」按 2 秒间隔抽帧（上限 10 帧）后调用识图模型把视频拆成分镜表，自动生成带连线的脚本节点（沿用脚本工作台分镜表结构）；「剪辑」弹窗内设置入点 / 出点后经浏览器 MediaRecorder 重编码导出片段，生成带连线的新视频节点。视频高清放大 + 补帧因模型注册中心暂无视频编辑 / 放大类能力未实现。
- 画布视频节点新增「主体库」（对齐 LibTV 主体库）：账号级可复用主体 = 名称 + 描述 + 多张参考图（上传至账号后端），视频节点 Composer 选择主体后，生成时主体参考图集并入参考图、主体描述追加进提示词，实现角色 / 商品跨镜头一致性。
- 画布图片节点「风格库」增强：预设扩充为 6 大分类 31 项（摄影写真 / 电商营销 / 动漫游戏 / 风格插画 / 3D 渲染 / 艺术设计），弹层支持分类标签切换与跨分类关键词搜索，并可将当前提示词保存为账号级自定义风格（随账号工作区同步，可在任意图片节点复用）。
- 画布图片节点图像工具集补全：新增「扩图」（六档目标画幅向外延展画面）、「打光」（主光方向 / 光色 / 强度组合重新打光）、「抠图」（提取主体替换纯色背景）、「720全景」（equirectangular 全景图）四个工具入口，蒙版「局部编辑」弹窗新增「AI 擦除」（涂抹区域内容移除并自然补全背景），均走图生图编辑管线生成新节点。
- 画布视频节点「运镜库」扩充为 24 项预设运镜（推 / 拉 / 摇 / 移 / 跟 / 环绕 / 升降 / 手持 / 变焦 / 甩镜头 / 荷兰角 / FPV / 希区柯克变焦 / 子弹时间等），图片节点 Composer 新增「摄像机控制」（相机型号 / 镜头类型 / 焦距 / 光圈），均以提示词片段形式在生成时追加、重选即替换。
- 画布图片节点新增「快捷功能」浮动菜单：镜头聚焦、焦点编辑、电影级光影矫正、角色三视图、画面推演（3 秒后 / 5 秒前），以当前图为参考走图生图编辑管线生成新节点。
- 画布视觉与交互按 LibTV × TapNow 调研对齐：主题 token 统一为近黑底色 + 青色高亮；dock「+」、双击空白、右键空白共用同一创建菜单（空画布双击此前无行为，本次补齐）；顶栏保存状态带图标且失败/离线可点击重试；Agent 输入框支持 @ 精确引用与「+」轻量引用画布节点；工作流工具箱改为带搜索/类型筛选的卡片网格；左下小地图开关补齐真实实现（点击/拖拽导航）；远景缩放时节点上方保留可读标题标签。
- 画布物理交互按 LibTV × TapNow × 小云雀调研修正：普通滚轮/双指滑动改为平移画布，Ctrl/Cmd+滚轮与捏合为指针锚点缩放（此前滚轮直接缩放）；新增右键拖拽平移（短按仍出菜单）；快捷键面板承诺闭环——新实现 Ctrl/Cmd+D 复制节点和连线、Tab 打开创建菜单、Ctrl/Cmd+Enter 运行选中生成节点，移除面板中未实现的条目；工作流模板插入自动避开现有节点；空画布新增「输入灵感」入口，提交后由在线 Agent 直接搭建初始创作工作流。
- 画布 Agent 能力重写：操作层从 8 种 ops 扩展到 22 种，在线 Agent 新增 14 个工具、本地 MCP Agent 新增 13 个工具——节点重跑、整组执行、图像编辑（多角度/扩图/打光/抠图/720° 全景）、快捷功能、本地图像处理（裁剪/切分/放大）、视频解析/剪辑/合成、打组/解组、模板保存/插入、脚本宫格分镜；Agent 可见的节点参数从 6 个键扩到 20 个；Agent 撤销改为多级栈并在在线面板新增撤销按钮；tool 循环上限提升至 8 步。
- 后端新增 Junli Studio（img.junliai.org）视频调用适配：`junli_openai` 协议由专用适配器接管 Sora 风格异步视频任务（multipart 创建请求转 JSON、参考图映射为 reference_images / 首尾帧、轮询与 /content 下载），图像端点保持通用 OpenAI 透传。
- 管理后台新增「默认模型」配置（文本 / 图像 / 视频 / 音频各一个，选已发布模型，可留空）：经 `/api/runtime-config` 下发，创作端未显式选择模型或本地选择无效时优先使用后台默认模型；删除模型自动清空默认引用，保存时无效默认项由后端兜底清空。
- 在线 Agent 接入 Toonflow 风格 Skill：「美术风格」（11 项）与「故事风格」（12 项）选择器注入系统提示词，Agent 生成的图片 / 视频严格遵循所选风格的色彩、质感与题材约束；skill 文本定义随前端按需加载，选项清单与本地 Agent 共用。
- 画布脚本节点重构（对标 LibTV 脚本节点 v2）：新增「AI 拆解」用文本模型把剧本解析为角色/场景/道具资产 + 分镜表（内容/景别/时长/角色/场景/机位/台词）；分镜表可编辑（字段修改后提示词联动、新增/排序/删除）；生成提示词自动引用资产描述，提升角色与场景一致性；分镜/宫格生成不再放示例占位图，创建节点后立即调用模型真实生成；在线/本地 Agent 生成脚本/分镜时改为创建脚本节点（canvasTool='script'）而非普通文本节点；Agent 创建节点未显式指定坐标时由画布自动横向流式避让，解决多次创建重叠/排列乱问题；脚本节点支持从上游文本节点读取剧本（「从上游导入」一键填入并拆解），拆解按「人物 → 道具 → 场景」提取资产再拆分镜；脚本节点升级为全屏工作台（对齐导演台）：拆成分镜只建节点不自动生成、逐镜微调后单独生成，资产可单独生成资产图，工作台内实时显示各镜/资产生成状态；chat/completions 纯文本消息 content 转字符串，兼容 DeepSeek 等厂商（修复脚本拆解 400）；ComfyUI 节点 Composer 增加工作流选择与能力模式（反推提示词/文生图/参考图生图/图生视频），Composer 能力改为两级筛选（文本/图像/视频 → 文生文·图生文 / 文生图·参考图生图 / 文生视频·图片生视频·全能参考生视频），工作流按后台配置分类过滤；后台能力分类同步两级分组。
