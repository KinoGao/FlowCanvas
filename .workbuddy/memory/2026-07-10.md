## 2026-07-10 工作日志

### 画布引擎迁移：@xyflow/react → LeaferJS

**已完成核心迁移**:

1. **安装 LeaferJS 依赖**: `npm install leafer-ui @leafer-ui/draw @leafer-ui/core`

2. **新建核心文件**:
   - `components/leafer-viewport.ts` — 视口工具（坐标转换、CSS transform、viewport clamp）
   - `components/leafer-edges.ts` — LeaferJS 连线渲染管理器（Bezier 曲线、选中状态、临时连线）
   - `components/leafer-canvas.tsx` — 主画布组件，替代 react-flow-canvas.tsx
     - 视口管理（滚轮缩放、拖拽平移、Space 键平移模式）
     - 节点拖拽 + CSS transform 定位
     - 连线创建流程（start→drag→end→connect）
     - 框选（LeaferJS 渲染选框）
     - 边点击目标（DOM overlay 实现）

3. **修改 canvas-node.tsx**:
   - 移除 `Handle` / `useUpdateNodeInternals` / `ReactFlowPosition` 依赖
   - `ConnectionHandleDot` 改为纯 DOM 实现，添加 `data-handle` / `data-handle-type` 属性

4. **修改 canvas-client-page.tsx**:
   - 移除所有 `@xyflow/react` 类型导入（Connection, Edge, EdgeMouseHandler 等）
   - 移除 `ReactFlowCanvas` / `ReactFlowCanvasNode` / `reactFlowCanvasEdge*` 导入
   - 移除 `CANVAS_NODE_TYPE` / `CANVAS_EDGE_TYPE` 等适配器常量
   - 新增 `LeaferCanvas` 导入 + 直接渲染 `CanvasNode` 组件
   - 新增 Leafer 兼容的 handler 函数（handleLeaferConnect/ConnectStart/ConnectEnd/NodeDrag）
   - 移除旧的 React Flow handler（handleReactFlowNodesChange/NodeDrag/SelectionChange/EdgeClick）
   - 添加边点击目标的 DOM 元素渲染

5. **清理**:
   - 删除 `react-flow-canvas.tsx` / `react-flow-canvas-node.tsx` / `react-flow-canvas-edge.tsx` / `react-flow-adapter.ts`
   - 从 `globals.css` 移除 `@import "@xyflow/react/dist/style.css"`
   - 从 `package.json` 移除并 uninstall `@xyflow/react`

6. **架构**: 混合模式
   - LeaferJS Canvas 层：视口背景、连线渲染、框选矩形
   - React DOM Overlay 层：节点内容（保留现有 CanvasNode 组件）、连接手柄、边点击目标
   - CSS transform 管理视口变换

**待办**: 用户需自行 build 验证，可能需要修复的类型问题：
- Dead `reactFlowNodes` useMemo 块已删除
- Dead `updateReactFlowNodePosition` 已删除
- `ReactFlowCanvasPage` 函数名仅保留为内部标识符

### 修复迁移后问题

1. **恢复连接创建菜单**: `handleLeaferConnectEnd` 现在接受可选的 `canvasPos` 参数，当连线拖到空白区域时弹出 `ConnectionCreateMenu`
2. **文件拖放支持**: 在 `LeaferCanvas` 中添加 `onDrop` prop 支持，重命名旧 `handleDrop` 为 `handleDropFiles`
3. **清理死代码**: 删除 `preventCanvasContextMenu`（功能已由 `onContextMenu` 覆盖）
4. **修复依赖警告**: `handlePointerUp` 缺少 `getCanvasPos` 依赖项
5. **更新 AGENTS.md**: 画布引擎从 `@xyflow/react` 改为 LeaferJS
6. **验证**: `web/src` 中零处 `@xyflow` 引用残留

### 全面代码审查与修复（第二轮）

**TypeScript 编译**: 零错误

**修复的 P0 问题**:
1. **连线无法连接到已有节点** — `connectionTargetNodeId` 永远为 null。修复：LeaferCanvas 的 `handlePointerMove` 中在连接拖拽时通过 `document.elementFromPoint` 检测目标节点，调用 `onConnectionTargetChange` 更新状态
2. **LeaferJS 画布不随窗口缩放** — 添加 ResizeObserver
3. **框选不实际选中节点** — 添加 `onSelectionBox` 回调，在 pointerUp 时计算与选框相交的节点

**修复的 P1 问题**:
4. **背景网格丢失** — `drawBackground` 实现了 dots/lines 两种模式，使用 LeaferJS Rect/Line 绘制
5. **`app.clear()` 清掉连线缓存** — `LeaferEdgeLayer` 新增 `reset()` 方法，背景重建后调用以强制重连所有 edge
6. **连接手柄 `onConnectStart` 双重触发** — 移除 `ConnectionHandleDot` 的 `onMouseDown`，统一由 LeaferCanvas 的 `handlePointerDown` 处理

**其他修复**:
7. **中键平移** — 支持 `event.buttons === 4` 触发 pan 模式
8. **`commitProjectChanges` 未定义** — 替换为 `historyPausedRef.current = false`，项目保存由 useEffect 自动处理
9. **`handleLeaferNodeDragStart` 未暂停历史** — 添加 `historyPausedRef.current = true`

---

### 全面代码审查（全项目，只读）

完成 FlowCanvas 全项目代码审查，覆盖前端（web/）、后端（backend/）、Canvas Agent（canvas-agent/）及部署配置。审查报告输出到 `code-review-report.md`。

**关键发现**:
- **前端**: canvas-client-page.tsx 6574行上帝组件是最大技术债务；DirectorThreeStage 巨型 useEffect；模块级全局状态内存泄漏
- **后端**: 硬编码默认鉴权码 + CORS 不安全配置；零输入验证；异常处理器覆盖不足
- **Agent**: 控制台输出 token；CORS 自动白名单；URL 参数传 token（三个严重安全问题）
- **部署**: VERSION 文件缺失导致 Docker 构建失败
- **全项目**: 零测试覆盖

前端 13 个文件超过 500 行，后端和 Agent 均无超 500 行文件。
