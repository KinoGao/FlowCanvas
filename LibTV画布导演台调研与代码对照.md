# LibTV 画布导演台调研 与 FlowCanvas 代码对照

> 调研时间：2026-07-08
> 目的：搞清楚 LibTV「画布导演台」是如何实现的，并对照 FlowCanvas 现有代码，找出对齐机会（只补创作流形态，不复制商业化能力）。

---

## 一、LibTV 画布导演台是怎么实现的

### 1. 定位：画布里的轻量级 3D 构图节点

导演台**不是独立软件**，而是嵌入无限画布的一个节点：

- 双击画布空白处 → 新建「导演台」节点 → 点「打开导演台」进入 3D 空间
- 用户像「摆积木」一样搭建故事空间，AI 演员位置、姿势都能准确控制
- 切换视角截图 → 得到能被 AI 模型准确理解的构图参考图 → 发回画布

### 2. 3D 导演台核心能力

- **添加模型**：人体素模（男女老少高矮胖瘦）、基础几何模型、群众阵列、本地上传模型
- **模型编辑**：每个模型可改名称、颜色，支持移动位置、旋转方向、缩放体积
- **导演视角**：选中摄像机 → 调整机位 → 切换「机位视角」实时预览
- **截图**：得到当下视角的准确站位分镜图，发送到画布作为参考图
- **进阶：720° 全景**：本地普通场景图一键转 720° 全景图，再叠加人物素模旋转视角，适配不同机位

### 3. 解决的核心痛点（为什么需要它）

- **多人场景一致性**：换机位 / 切镜头时 AI 不再乱改人物站位（如五人会议镜头把男主变成边缘人）
- **不同比例人物**：锁定比例（如机甲反派 = 男主 1.7 倍身高），摆出战斗关键帧站位 / 姿势，得到符合比例的分镜图
- **走位调度**：近大远小、交叉走位等用文字描述是在「赌运气」，在 3D 空间摆好位置生成多张定位截图更可控

### 4. 配套的分镜能力（与导演台协同）

- **分镜组**：多选多张分镜图 → 「合并到分镜组」→ 自动收纳进宫格、按剧情顺序排好 → 一键导出带序号的 4K 大图（团队协作用）
- **`/` 快捷功能**（图像生成器内、有参考图时）：多机位九宫格、剧情推演四宫格、25 宫格连贯分镜、电影级光影矫正、角色三视图生成、画面推演（-3 秒后 / -5 秒前）
- **宫格切分**：9 宫格 / 25 宫格图像拆切

### 5. 与节点 / 工作流的关系

- 导演台节点产出的分镜参考图 → 喂给图片 / 视频节点作为参考图
- **脚本节点**：上传剧本 / 角色 / 参考视频 → 自动拆分成脚本 + 对应分镜图
- **工作流**：打组 `Ctrl/Cmd + G` → 「创建工作流」保存 → 左侧「我的工具箱」调用 → 整组执行（一键重跑前后有逻辑关系的节点）

---

## 二、FlowCanvas 现有实现对照（来自代码探索）

### 已有能力（对标基础较好）

| 能力 | 实现位置 |
| --- | --- |
| 画布页面 + ReactFlow 节点系统 | `canvas/page.tsx`、`canvas/[id]/canvas-client-page.tsx` |
| 通用节点组件（按 `metadata.canvasTool` 分支：script / videoComposition / director） | `canvas/components/react-flow-canvas-node.tsx`、`canvas-node.tsx` |
| **3D 导演台**（THREE.js） | `DirectorStudioOverlay` + `DirectorThreeStage`，含机位 `directorShots`、场景设置、角色姿态可拖拽、镜头取景 `CameraHelper` |
| 导演台节点 | `createDirectorNode`（`canvasTool:"director"` → "导演台"） |
| 分镜拆出（按 beats 拆镜） | `createDirectorStoryboard` / `createScriptStoryboard`、`ScriptDeskPanel` |
| 工具栏 / 主题 | `canvas/components/canvas-toolbar.tsx`、`lib/canvas-theme.ts` + `stores/use-theme-store.ts` |
| 批量生成 / AI 助手 / 连线 / 拖拽 | `canvas-node.tsx` 批量 props、`canvas-assistant-panel.tsx`、各类 `onNodeDrag*` / `handleReactFlowConnect` |
| 工具箱（预设拾取器） | `canvas-toolbar.tsx` 的 `CanvasToolboxModal`（含「可复用」标签，但属预设拾取，非保存模板） |

> ⚠️ **代码实测结论（2026-07-08 复核）**：3D 导演台对「单角色 + 多机位取景 + 截图回画布」主路径**确实可用**（真实 WebGL 渲染、入口已接通、子组件/常量齐全可编译）。但存在明确限制：
> 1. **仅支持单个角色**——`addMannequin` 只拼 1 个人，`DEFAULT_DIRECTOR_CHARACTER` 是单对象，无多角色/群众阵列/本地上传模型（LibTV 核心卖点"多角色换机位一致性"完全做不到）。
> 2. **工具栏大量按钮是占位空壳**——只有"添加机位""截图"绑了 onClick；"添加角色/全景图720/画幅比例/AI识图/导入/全屏/重置视角"点了无反应。
> 3. **疑似 HiDPI 画布尺寸 bug**——`renderer.setSize(w,h,false)` 未设 canvas CSS 尺寸，视网膜屏（devicePixelRatio>1）上画布可能被裁切，需浏览器实跑确认。

### 差距对照表（对齐机会）

| 能力 | LibTV | FlowCanvas | 状态 |
| --- | --- | --- | --- |
| 3D 导演台（单角色取景） | ✅ 完整（多角色/素模/全景） | ⚠️ **部分可用**：仅单角色 + 多机位，含可拖拽素模/取景框/截图回画布；但「添加角色/全景图720/画幅比例/AI识图/导入/全屏/重置视角」按钮均为未实现占位，且疑似存在 HiDPI 画布尺寸问题 | 未完全对齐（多角色是核心缺口） |
| 分镜拆出（脚本→分镜） | ✅ | ✅ `createScriptStoryboard` | 已基本对齐 |
| 分镜组宫格编组 + 一键导出 4K 大图 | ✅ | ❌ | **缺** |
| 9 / 25 宫格连贯分镜 + 多机位九宫格 | ✅ | ❌（`/` 快捷功能雏形） | **缺** |
| 画面推演（-3 秒后 / -5 秒前） | ✅ | ❌ | **缺** |
| 角色三视图生成 | ✅ | ❌ | **缺** |
| 720° 全景图 | ✅ | ❌ | **缺** |
| 工作流打组（`Ctrl+G`）+ 保存为模板 + 工具箱复用 | ✅ | ⚠️ 仅预设拾取器 | **缺**（v1 已列待办） |
| 节点逻辑打组 | ✅ `Ctrl+G` | ❌ 仅批量生成分组 | **缺** |

---

## 三、结论与建议

### 现状判断
FlowCanvas 的 **3D 导演台是可用底座，但不是"最好基础"**——`DirectorThreeStage` 基于 THREE.js，已有机位、场景、角色姿态拖拽、镜头取景、截图回画布，与 LibTV「摆积木」式 3D 构图思路**方向一致**。但当前只支持**单角色**，缺 LibTV 最核心的"多角色换机位一致性"能力，且 UI 有一批未接功能的按钮。若要做多人/机甲/会议等复杂构图，必须先补**多角色系统**这一块。分镜「拆出」有雏形。

### 建议优先补齐（在 v1 范围内，不复制商业化能力）

1. **分镜组宫格编组 + 一键导出 4K 大图**：解决「图一多画布就乱、关键帧找不到」的痛点，团队向强需求。
2. **`/` 快捷功能补齐**：多机位九宫格、25 宫格连贯分镜、画面推演（-3 秒后 / -5 秒前）、角色三视图。与 AGENTS.md「通过 `/` 快捷功能逐步对齐」的方向一致。
3. **工作流：打组 + 保存为模板 + 工具箱复用**：将现有预设拾取器升级为「保存当前工作流为模板、可复用」，这是 v1 待办项。

### 不建议对齐（不在 v1 范围）
- 社区、分享、积分、付费模型、发布审核 —— 平台属性，不属于开源单机工具 FlowCanvas。

### 注意点
- LibTV 的「导演台 / 脚本节点 / 工作流 / 工具箱」等术语在 FlowCanvas 中已有对应或接近实现，补充时**保持语义一致**，不要重新定义。
- 引入新依赖前先检查 `web/package.json` 是否已有同类（如 3D 已用 THREE.js，分镜宫格可复用现有 canvas 组件而非另起一套）。
