---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

## UI 工作台化改造（对标 TapNow 风格）

- 全局主题：亮色背景由纯白改为灰白层次（工作台底色），暗色背景加深，边框/输入框/卡片重新分层；圆角从 10px 收紧为 8px；滚动条统一为细窄中性色；文字选区颜色统一。
- antd 组件 token 对齐：8px 圆角、32px 紧凑控件高度、边框/浮层/阴影按新中性灰阶重设。
- 顶部导航：高度从 64px 收紧为 56px，激活态下划线改为圆角短条，文字/边框全部改为主题变量（随深浅主题自动切换）。
- 登录/工作区锁定页：从纯文本居中改为卡片化（圆角边框 + 背景卡片 + 圆形图标），加载/失败/未登录三态统一。
- 列表页（画布库/生图/视频/提示词/素材）：页面根容器统一为主题背景与前景色，消除页面间明暗不一致。
- 需要确认：亮/暗主题切换后导航、列表页、弹窗、按钮视觉统一；画布内节点、工具栏、面板显示正常；移动端导航抽屉正常。

## 安全与可靠性加固

- **ComfyUI 代理 SSRF 修复**：未登录的 `/api/comfyui-proxy?path=/view` 媒体读取不再接受客户端传入的 `baseUrl`，只允许访问配置的 `COMFYUI_BASE_URL`（未配置时回退 127.0.0.1:8188）；已登录用户不受影响。需要确认：未登录时用自定义 baseUrl 访问 `/view` 被拒绝；登录后 ComfyUI 面板的媒体预览 / 任务提交 / 上传均正常；未配置 COMFYUI_BASE_URL 的本地部署行为不变。
- **模型运行时鉴权收紧**：`/api/model-runtime/models/` 与 `/api/model-runtime/providers/` 仅 GET（浏览器媒体加载）公开，POST 模型调用必须登录。需要确认：未登录调用模型接口返回 401；画布生成（图片/视频/音频/文本）登录后正常；`<img>`/`<video>` 直接加载生成媒体仍无需登录。
- **默认凭证恢复硬编码（内部部署）**：`AUTH_CODE` 默认 `gycode`、`ADMIN_CODE` 默认 `admincode`、`MEDIA_SIGNING_SECRET` 默认继承 `ADMIN_CODE`（均可通过环境变量覆盖），不再随机生成。需要确认：不设置任何环境变量时注册（authCode=gycode）与管理后台登录（adminCode=admincode）均可用；设置环境变量后覆盖生效；Compose 部署开箱即用（不再强制要求三个环境变量）。
- **docker-compose 凭证**：两个 compose 文件默认使用硬编码凭证，可通过环境变量覆盖。需要确认：直接 `docker compose up -d` 可拉起；后端镜像由 CI 一并构建（tag `v*` 同时产出前端与后端镜像）。
- **任务去重锁修复**：同用户同任务 ID 的并发请求不再绕过去重（锁对象引用计数）。需要确认：视频生成任务重复提交（如快速双击）只创建一个任务；并发轮询任务状态不产生重复生成。
- **上游请求重定向关闭**：模型适配器（OpenAI 通用 / Agnes / Seedance）不再跟随 3xx 重定向，代理错误统一返回脱敏文案。需要确认：正常模型调用不受影响；上游返回 5xx/超时时前端提示为通用错误信息。
- **媒体下载响应加固**：`/api/user/files/...` 响应增加 `X-Content-Type-Options: nosniff`，contentType 白名单（image/video/audio/pdf/json/octet-stream/text）之外的按 `attachment` 下载返回。需要确认：画布图片/视频/音频节点显示正常；上传非白名单类型文件后下载行为符合预期。
- **机位面板崩溃修复**：摄像机属性面板的 hooks 顺序违规（选中机位失效后再选中会触发 React "Rendered more hooks" 崩溃）已修复。需要确认：删除当前机位后再选中其他机位，右侧面板正常；机位属性编辑、截图、发送到画布功能正常。
- **Director 拖拽性能**：机位/角色变换拖拽期间不再每帧执行全量状态深拷贝、JSON 比较与 localStorage 写入（批量结束时统一落盘一次）。需要确认：拖拽机位或角色位置时画面流畅无卡顿；拖拽结束后刷新页面变换结果保留；撤销（Ctrl+Z）行为与之前一致。
- **选择同步防抖**：画布节点选择状态未变化时不再触发 React 重渲染。需要确认：框选/点选/连选节点行为与之前一致；选中态高亮正常。

## 脚本分镜表视图

- 脚本节点工作台的右侧「分镜预览」已升级为表格化「分镜表」：每行显示序号、景别（从正文关键词推断：大远景/远景/全景/中景/近景/特写）、时长（默认 3s，正文含「N 秒」时取 N）、画面描述与标题，每行有独立「生成」按钮可单独把该分镜生成图片节点并连线。需要确认：脚本正文修改后分镜表实时更新；点「生成」只创建对应一个分镜节点并自动连线；「拆成分镜」仍一次性创建全部分镜；刷新画布后 scriptBeats 的景别/时长字段保留。

## 快捷分镜浮动菜单（节点上方）

- 文本节点 / 脚本节点悬停时，节点上方的浮动工具栏出现「快捷分镜」按钮（不再是 Composer 内键盘 `/` 触发），点击展开宫格命令菜单（四宫格 2×2、九宫格 3×3、25 宫格 5×5）；选择命令后以该节点内容为脚本按 rows×cols 网格布局批量创建分镜图片节点（每个分镜带机位变体提示词：大远景/远景/全景/中景/近景/特写/大特写/俯拍/仰拍/过肩镜头），全部自动连线到源节点。需要确认：悬停文本/脚本节点显示「快捷分镜」按钮，点击弹出宫格菜单；图片/视频/音频节点不显示该按钮；选择四宫格创建 4 个、九宫格 9 个、25 宫格 25 个节点且布局为网格；空脚本正文时用默认骨架生成；菜单随节点 hover 正常显隐、不遮挡节点；刷新画布后命令生成的节点和连线保留。

## 跨画布复制（带连线）

- 画布内复制粘贴（Ctrl+C/V、Ctrl+D「复制节点和连线」）的剪贴板从组件内 useRef 提升为模块级单例：在 A 画布复制一组节点（含内部连线）后切换到 B 画布，Ctrl+V 能把节点组粘贴到 B 画布居中位置，内部连线按新 id 重建，图片/视频/音频节点的 storageKey 媒体引用跨画布仍有效（后端账号存储全局共享）。需要确认：画布 A 复制 → 切到画布 B 粘贴成功且连线正确；粘贴后媒体正常显示（后端模式）；复制内容在画布 A 内粘贴行为与之前一致（居中、选中、打开 composer）。

## 工作流工具箱

- 工具栏新增「工具箱」按钮，打开工作流工具箱弹窗：选中画布上一组节点后可输入名称保存为模板（节点位置按选中组中心归一化，内部连线保留）；模板列表展示节点数/连线数，可「插入」到画布居中位置（新 id + 连线重建）或删除。模板存于当前画布项目（workflowTemplates 字段），随项目自动保存到后端账号工作区。需要确认：保存模板后刷新画布模板仍在；插入模板节点位置居中且连线正确；图片/视频模板插入后媒体正常显示；删除模板后刷新不恢复；空选中时「保存选中」禁用。

---

## OpenAI 图片响应格式

- OpenAI 兼容图片模型现在会把“生图响应格式 = URL”明确发送为 `response_format: "url"`，文生图、参考图生图和 multipart 图片编辑不再仅仅省略 `b64_json` 后回退到厂商默认值；返回的 `data[].url` 继续由现有图片响应解析器接收。需要确认 Junli 等默认返回 Base64 的中转服务在选择 URL 后实际返回临时媒体地址，并验证 Base64、自动模式和 Seedream 输出格式不受影响。

## Agnes 参考图兼容

- Agnes 图生视频上传的参考图会在后端生成独立的兼容副本：最长边限制为 2048px、透明背景合成为白色，并编码为不超过 5MB 的 JPEG；画布素材原图和其他模型的参考图处理保持不变。需要使用原先触发 `image URL could not be downloaded or did not return a valid supported image` 的大尺寸图片重新生成，确认 Agnes 能创建任务并正常返回视频。

## Seedream Runtime Contract

- Seedream 5.0 Pro now declares one output, up to 10 reference images, 1K/2K and interactive editing. Seedream 5.0 Lite, 4.5 and 4.0 declare up to 14 reference images, a 15-image combined input/output ceiling, and sequential image generation. For those group-image models, one selected output sends `sequential_image_generation: "disabled"`; two or more selected outputs send `auto` with `max_images`. Confirm the published runtime capability response, the Composer choices, and actual vendor responses with the configured Volcengine account.

## Video Result Storage

- Generated video and audio files now bypass the JSON/data-URL upload route and use multipart storage directly. This avoids the 20MB JSON parser ceiling and the base64 expansion that could add an avoidable failed upload after a provider task had already completed. Confirm with a generated video larger than 15MB that it reaches its node, remains playable after reopening the canvas, and no JSON-size warning is written by the backend.

- Resuming a persisted video task now always performs one provider status lookup before enforcing the local 30-minute deadline. A completed provider task can therefore still be restored after the canvas was closed; a task that is still queued or running after the deadline is ended as before. If the provider result is ready but saving it to the account media library fails, the task ID is retained for the next canvas open instead of submitting and charging for another generation.

- 视频任务创建请求现在必须在 90 秒内返回任务 ID，Agnes、Seedance 和通用 OpenAI 视频协议的后端适配器使用相同上限；任务创建成功后的轮询、恢复和最终媒体下载仍沿用原有 30 分钟生成窗口。需要分别确认上游不可达时能在 90 秒左右明确失败，以及正常创建后长时间排队的任务不会被创建超时误杀。

## Model Protocol Catalog

- The administrator model drawer now selects a backend-provided calling protocol instead of typing a free-form adapter name. OpenAI direct, Gemini, Agnes Video V2, and Seedance 1.0/1.5/2.0 Ark task protocols are listed from the registered backend adapters. Adding a model should only require selecting one of these protocols plus its capabilities; a truly new vendor HTTP contract still needs one new adapter, which then appears in the selector automatically. Confirm the catalog loads in `/admin`, existing model protocol values are retained, and saving/verifying a model uses the selected protocol.

- Seedance 1.0 Lite runtime entries now use the Seedance task adapter rather than the generic OpenAI video path. The adapter also permits explicitly disabled unsupported flags such as `watermark: false`; only an unsupported enabled feature is rejected. Confirm Lite text-to-video and image-to-video create a task, reach a terminal state, and do not fail before task creation because of disabled audio, watermark, or draft fields.

- Official Seedance model IDs now reapply their authoritative protocol and capability template when runtime configuration is loaded, even if an older saved model contains an empty capability object or the generic `openai` adapter. Confirm existing Seedance 2.0 configurations publish as `seedance-v2` without requiring the administrator to recreate the model.

## Canvas Grid Snap

- The canvas appearance panel now provides a per-project grid-snap toggle. When enabled, newly created nodes and drag operations align to the 56px canvas grid; the setting is saved with the project and participates in undo/redo. During dragging, edge and center alignment guides appear for nearby nodes and take priority over the grid on the matching axis. Confirm single-node and multi-node dragging, node creation from the toolbar/context menu, reopening the canvas, and undo/redo with the toggle both enabled and disabled.

## Canvas Minimum Zoom

- 画布滚轮、加减按钮和快捷键统一按当前比例的 20% 递增或递减（例如当前为 20%、40%、70% 时，一次放大约增加 4%、8%、14%）；手动百分比输入、旧画布视口恢复和适合屏幕统一限制在 `20%–125%`。需要确认所有缩放入口均不会越界，滚轮缩放保持以鼠标位置为中心，旧项目中超过范围的缩放值会自动收口。

## Leafer Canvas Interaction Engine

- Leafer 拖动和连线仍按动画帧直接更新视觉位置，React 节点状态改为约 10fps 同步并在松手时强制提交最终位置；未选中的 DOM 节点保持挂载，同时允许浏览器跳过视口外绘制。需要在 100 个以上节点的画布中确认拖动连续、连线不脱离、松手后位置准确且媒体不会重新请求。
- 已有画布恢复遮罩会等待当前视口内的图片在 Leafer 中解码完成或明确失败后再淡出，最长等待 4 秒；相同后端媒体的并发签名 URL 请求会合并为一次。需要确认刷新后不再出现空节点闪烁、同一素材不再重复请求签名接口，并验证坏图不会永久阻塞画布。
- 图片和视频 Composer 的展开按钮现在可切换输入区高度；视频多模态模式在已有图片或视频引用时允许不填写文本提示词，纯文生视频仍要求提示词。需要确认首帧、首尾帧和多模态参考模式可空提示词提交，普通文生视频不可空提交。

- 画布已按 LeaferJS 的 `App + ground/tree/sky + Editor` 分层：`ground` 绘制背景与网格，`tree` 管理节点外壳、静态概览、连接线和命中区域，`sky` 绘制辅助线与临时连接线；React 仅保留视频、音频、输入框、Composer 等必须依赖 DOM 的原生交互控件和业务状态。需要确认单选/多选、框选、组移动、缩放、平移、节点改名、文本编辑和媒体控制均保持原行为。
- 单击选中普通节点时会同时保持节点操作菜单和 Composer 可见，图片内容节点也不再需要额外点击才能打开 Composer；被动打开不会自动聚焦或选中 Composer 内容，用户点击输入区后才进入编辑。需要确认鼠标移出节点后菜单不会消失，多选、组节点和导演台仍保持原有交互。
- 所有持久连接线已从 React SVG 迁移为 Leafer `Path`。节点移动和缩放时，只有端点变化的连接线会在动画帧内更新；节点、文本和连接线均使用几何/样式签名跳过未变化对象的重复 `set`。需要在 100 个以上节点的真实画布中确认单节点、多选和组拖动时连线持续贴合，且不再出现全量连接线路径重算。
- 视频控制层只保留进度条和按钮的实际命中区域，其他透明区域继续用于 Leafer 节点拖动；节点拖动和缩放事件会同步更新 DOM 媒体层及相邻连接线，选中节点、选中连线或悬停连线时恢复动态导电粒子。需要确认有内容和无内容的视频节点均可从非控件区域拖动，播放/进度/音量仍可操作，拖动过程中连线无脱节。
- 高频滚轮、平移和节点变换先直接更新 Leafer/DOM 展示层，再按受控频率同步 React；40% 以下的未选中文字、配置和 ComfyUI 静态内容由 Leafer 文本层绘制，图片在 Leafer 确认解码成功后才隐藏原 DOM 图像。需要确认 20%-50% 缩放下无空白图片、无缩放闪烁，选中节点后编辑控件立即恢复。
- Canvas appearance settings now expose an independent `辅助基准线` switch next to grid snapping. It defaults to enabled for existing projects; when disabled, node dragging skips alignment scanning, guide rendering, and guide snapping while optional grid snapping remains active. Confirm the setting persists after reopening the canvas and participates in undo/redo with the other canvas appearance settings.

## Canvas Media Residency

- Canvas nodes are mounted once when the project opens and are no longer added or removed according to the current viewport. Leafer panning and zooming only transform the canvas layer; image, video and audio elements do not use viewport `content-visibility` pruning, and audio remains mounted even when its low-zoom summary is rendered by Leafer. Collapsed batch children remain the only intentionally hidden nodes. Confirm on a large canvas that repeated panning and zooming preserve media readiness, video playback position, audio state, text editing state, and stable request counts.

## Canvas Node Panels

- Empty text, image, and video nodes now expose a compact spotlight card with node-specific identity, guidance, and action rows; ComfyUI keeps its existing controls inside the same material system. Confirm light and dark themes, image/video readability, node dragging, resize handles, selection, and connection creation on both dense and sparse canvases.

## Canvas Generation Run Ledger

- Generation nodes now persist up to six recent runs in node metadata, including the run ID, model, mode, prompt snapshot, status, and error details. The Composer exposes the current run state, refresh-resume hint, recent run list, and a direct retry action for failed runs; the loading node also makes the refresh-resume behavior visible. Confirm text, image, video, audio, and ComfyUI runs across success, failure, stop, refresh, and reopen flows, and verify that a retry creates a new run without removing the previous record.

## 节点圆角与交互反馈

- 文本、图片、视频、音频、ComfyUI、配置和组节点统一使用 8px 圆角，React 内容层与 Leafer 绘制层共用同一轮廓，媒体内容会沿节点边缘裁切。悬停、选中、连接目标和拖动分别提供轻微提亮、强调描边与按层级增强的阴影，视觉更新只作用于当前交互节点，不触发整张画布 React 重渲染或常驻动画。需要确认浅色/深色主题、图片与视频边缘、批量图片叠层、组节点、节点拖动和连接吸附状态下圆角与反馈均保持一致，并在百节点画布中观察帧率没有明显下降。

## 已有画布刷新稳定性

- 画布恢复改为单一常驻渲染窗口：后端工作区正常加载和项目媒体恢复共用同一个前台缓冲层，不再先切换通用加载页；真实节点、连线、背景模式和保存视口在异步恢复完成后作为同一批状态挂载到后台画布。恢复层进入时立即不带动画地盖住旧窗口，只在 Leafer 节点和视口完成首帧后单向淡出，并使用纯主题底色避免自身网格与项目背景再次切换；只有未登录或后端工作区恢复失败时才进入对应锁定页面。自动回归已连续采样单节点硬刷新和三节点画布进入过程，第一次出现 Leafer 时节点数量与最终 transform 已完整就位，淡出期间未再次变化。需要继续确认深浅主题、画布间直接切换、空白新画布和包含大量媒体节点的画布均不会出现闪烁，恢复失败或会话过期仍显示原有错误页。

## 图片 Composer

- 图片节点 Composer 改为独立宽面板：顶部按引用顺序展示图片、文本等素材缩略图并可移除引用，中央为大尺寸提示词输入区，底部集中提示词库、`@` 素材引用、风格、模型、画质、清晰度、比例、生成数量和生成按钮。图片参数继续读取后台发布的模型能力；风格只在提交任务时追加，不修改用户原始提示词。需要确认浅色/深色主题、窄屏横向滚动、不同厂商能力、图生图引用、移除引用连线，以及刷新后节点级风格选择仍能保留。

## 视频 Composer

- 视频节点 Composer 改为独立宽面板：顶部按引用顺序展示媒体缩略图并可移除引用，中央为大尺寸提示词输入区，底部集中模型、比例、时长、风格、`@` 素材引用、生成模式、运镜、高级设置和生成按钮。比例、时长、分辨率、声音、水印等继续读取后台发布的真实模型能力；风格和运镜作为可见的提示词增强预设，仅在提交视频任务时追加，不改变用户原始提示词。需要确认浅色/深色主题、窄屏横向滚动、各厂商能力差异、首帧/首尾帧/多模态引用校验、移除缩略图后对应连线消失，以及刷新后节点级风格和运镜选择仍保留。

# 待测试
- 后端 OpenAI 兼容音频模型：后台模型分类新增“音频”，可为每个音频模型声明 `text-to-speech`、可用音色、输出格式、语速和是否支持语音指令；只有通过验证且发布的音频模型才会进入画布的音频模型列表。画布音频节点请求仍使用 `/audio/speech`，后端会注入真实 `requestModel` 和厂商 API Key，并拒绝错误分类、空输入、不支持的音色/格式/语速或指令参数。需要确认：添加一个 OpenAI 兼容 TTS 模型后能在画布音频节点选择，`mp3/wav` 等输出可保存并刷新恢复；配置为不支持的选项时前端得到可读错误；视频、图片和文本模型调用不会受到影响。
- Composer 视口自适应：Composer 会测量实际内容高度，优先在目标节点可用空间更大的一侧打开，并为顶部状态栏和底部 Dock 保留安全间距；可用空间不足时 Composer 自身滚动，不再覆盖目标节点、生成按钮或越出当前窗口。需要确认：短提示词、超长提示词、不同缩放倍数、靠近视口上下边缘的节点和窄屏窗口均能保持可读、可滚动、可关闭。
- Composer 引用素材预览：引用条中的图片会先按 `storageKey` 解析为可显示的媒体地址，再显示小缩略图；鼠标悬停或键盘聚焦时，图片显示大尺寸预览，文本显示完整内容预览。引用条仍按连线创建顺序排列，不展开时保持紧凑。需要确认：后端账号媒体、首次加载的本地媒体和生成图片均能显示；图片加载失败时回退图标；长文本换行且不遮挡 Composer 的操作控件。
- 画布节点尺寸、落点与删除快捷键：文本、音频、ComfyUI 等普通节点继续使用稳定的默认尺寸；图片和视频节点按原始比例适配，并受最小可识别尺寸与最大预览尺寸约束，手动缩放同样不会无限放大。工具栏新增节点会落在最后一次点击的画布位置（首次未点击时落在当前视口中心），右键菜单新增节点会精确落在右键位置。选中一个或多个节点后按 `Delete` 或 `Backspace` 可直接删除，焦点位于输入框、文本编辑区或 Composer 内时不触发删除。需要确认：空白节点、上传图片/视频、生成产物、右键新增和工具栏新增均落点正确；极小/超大图片比例正常且尺寸受限；多选删除、连接删除和文本编辑时的退格行为互不冲突。
- 图片 Composer 画质、清晰度与生成数量：Composer 同时提供“低 / 标准 / 高画质”和 `1K / 2K / 4K` 清晰度；最终尺寸完全由清晰度与比例决定，不再提供 W/H 自定义或 `customSize` 能力字段。模型能力 JSON 的 `qualities`、`resolutions`、`ratios` 与 `counts` 会分别限制对应控件，生成时画质作为模型质量参数传递，清晰度与比例转换为请求尺寸；Seedream 保留其官方 `1K / 2K / 4K` 尺寸参数。生成数量使用紧凑的 `1 / 2 / 4` 优先控制和数值输入，模型声明固定 `counts` 时只显示允许数量，新画布默认 1 张。需要确认：不同厂商下不支持的画质、清晰度、比例均不可选；`1K / 2K / 4K` 与比例组合能产生正确尺寸；重试生成保留画质和清晰度；生成数量实际与选择一致。

- 画布节点命名与引用顺序：从工具栏、右键菜单和节点连线创建的文本、图片、视频、音频、ComfyUI 节点会按各自类型的画布创建顺序获得默认名称，例如“文本节点 1”“图片节点 1”“ComfyUI 节点 1”；双击节点顶部名称可修改，刷新后应保留自定义名称。文本、图片、视频、音频引用连接到生成节点后，Composer 和 ComfyUI 面板中的引用条应按连线创建顺序展示。用户菜单、视频合成和导演台不再创建旧“生成配置”节点，改为独立 ComfyUI 类型；历史生成配置节点仍可正常打开和运行。
- 视频生成统一 30 分钟截止：Agnes、Seedance 与其他视频模型从任务开始起最多等待 30 分钟，截止后会中止当前轮询，画布节点结束加载并进入错误状态，视频生成记录保存为“失败”，统一提示“视频生成超过30分钟，任务已自动结束。”；重新打开页面时，超过期限的历史“生成中”任务会立即收口，不再继续等待或接收后续上游结果。需要确认画布 Composer 和视频页面都能在超时后恢复操作，未超时任务、手动取消和上游主动失败仍保持各自原有行为。
- 后台管理长内容滚动：管理后台改为独立的全视口纵向滚动容器，模型配置、ComfyUI、工作流和账号画布内容超过屏幕高度时可以继续向下滚动，并为页面底部保留操作空间。需要确认桌面和窄屏下最后一项配置、保存按钮及各标签页内容均可访问。
- 画布生成等待动效替换：生成中节点使用静态点阵、局部点亮遮罩与双向淡入淡出柔光，替代原有单向扫光。需要确认图片、视频、音频和文本节点在生成等待、画布缩放及节点拖拽时动画正常，生成完成后动效及时消失。
- 后台全局模型注册中心与管理员权限：管理员先配置厂商名称、接口地址、API Key、协议和模型列表路径，再拉取上游模型并逐个分类为文本、图像或视频。文本模型可配置纯文本与识图/多模态；图像模型可配置文生图、图生图、图像编辑、画质、1K/2K/3K/4K、比例和 1/2/4 张数量；视频模型可配置文生视频、全能参考、图生视频、首尾帧、图片参考、智能多帧、有声、水印、草稿、时长、分辨率、数量和引用上限。创作端只接收稳定模型 ID 与对应类别能力，请求由后端映射到私有 `requestModel` 并再次校验。需要确认：OpenAI 兼容和 Gemini 协议均能拉取模型；同一厂商可混合配置三类模型；切换分类时不会遗留无关能力；Agnes、Seedance、可灵等模型只显示和接受各自支持的参数；`/api/runtime-config` 不泄露 API Key、真实地址或 `requestModel`；普通账号不能访问管理接口。
- Seedance / Seedream 厂商协议适配：Seedance 视频创建、任务查询和视频下载统一走后端 `/api/model-runtime/models/{id}/videos`，后端按 `requestAdapter` 将请求转换为火山 `/api/v3/contents/generations/tasks`，并按官方模型能力强校验场景、媒体角色和顶层参数。Seedance 2.0 支持文生视频、首帧、首尾帧与多模态参考，限制最多 9 张图片、3 个视频、3 段音频且音频不可单独输入；标准版支持 4K，Fast/Mini 仅支持 480P/720P，并拒绝 `frames`、`seed`、`camera_fixed`、`service_tier`。Seedance 1.5 Pro 支持有声、首尾帧、4~12 秒或智能时长 `-1`，样片模式必须使用 480P 且不允许返回尾帧或离线推理；Seedance 1.0 Pro 支持首尾帧，1.0 Pro Fast 仅支持文生视频和首帧，二者时长均为 2~12 秒。完成后的临时视频 URL 会转成同源二进制响应，本地参考图片 Data URL 会先落到后端公网图片目录；本地 MP4/MOV 参考视频会先通过当前登录账号上传到后端公网媒体目录，再把公开 URL 传给火山，遗留的视频 Data URL 也会由后端转换，避免向 Seedance 发送官方不支持的视频 Base64；WebM 和超过 200 MB 的参考视频会在请求上游前被拒绝。Seedream 5.0 lite 能力目录和图片能力校验增加 3K。需要使用真实火山账号确认各模型实际 ID、任务创建、轮询、有声/无声、分辨率和媒体参考组合；完成产物还需确认能在画布下载、保存和刷新恢复，火山上游返回 400/5xx 时前端应显示可读错误且不暴露 API Key。本次自动测试不调用付费生成接口。
- 视频能力配置由管理员手工选择的选项作为权威值，读取或保存时不会用官方模板覆盖。后台与 Composer 将单图 `first_frame` 能力明确显示为“首帧图生视频”，与“首尾帧图生视频”分开。需要确认：保存后重新打开后台，首帧、首尾帧及其数量限制保持不变；发布后画布菜单显示相同选项并按配置校验。
- 图片节点标题与分辨率：图片节点左上角固定显示图片图标与可双击重命名的“图片节点 N”，右上角显示生成或导入图片的原始像素宽高；不再在图片内容右下角重复显示像素和文件大小。需要确认：图片生成、导入、刷新恢复及缩放画布后标题和分辨率仍保持在节点外侧上方。
- 上传图片进入 360 全景预览：图片节点只保存本地或后端 `storageKey`、没有内联 `content` 时，点击节点工具栏“360视图”会完成标记并立即进入沉浸式预览；已标记图片再次点击也会直接打开。三击图片进入预览的手势继续保留。需要确认本地图片、后端账号图片均可进入，左键旋转、滚轮调整 FOV、截图插入画布正常，普通图片三击查看不受影响。
- 拖出连接线后的固定预览：从节点左侧或右侧连接点拖到画布空白处并松开时，临时 SVG 连接线会保留在松开位置，线长和方向不再随之后的鼠标移动变化；画布缩放或平移后仍锚定原画布坐标，并与节点创建菜单保持同步。按 Esc、关闭菜单、连接到已有节点或创建新节点后会清除临时线。需要确认：源端口和目标端口都可正常使用，连续取消/创建不会留下残线，也不会触发画布拖拽或 `Maximum update depth exceeded`。
- LibTV 连接点 / 连接线 / 导演台细节对齐：连接点应固定在节点左右边缘中点，节点边缘显示 LibTV 式小加号，连接线 hover 时出现短暂流光，选中时出现持续流光；拖拽新连接线时也应从对应左右端口出发。连接线靠近有效目标端口 48px 范围时会吸附到端口中心并高亮候选端口，已吸附后移动到 64px 外才解除，正向 source -> target 与反向 target -> source 均可使用。导演台摄像机和角色属性面板都应提供左/右/上/下/前/后 6 向移动按钮，角色面板继续保留头部、躯干、左右臂、左右腿骨骼姿势控制。需要确认缩放、拖拽节点、hover 连接线、选中连接线、正反向端口吸附、打开导演台、调整角色/机位位置后都没有 `Maximum update depth exceeded`。

- 后端账号画布软删除与多开防护：`/api/user/projects` 使用按项目 ID upsert，不会因为一次 payload 缺少某个项目而删除后端记录；删除画布写入 `projectTombstones` 并同步为后端 `deleted_at` 软删除。登录态在刷新后保留，只有 `/api/auth/me` 明确返回 401/403 才退出；同一画布 ID 被另一个窗口占用时，后打开的窗口显示“会话已过期”且不进入保存逻辑。需要确认：删除后刷新不显示但数据库仍保留记录和 `deleted_at`；后端短暂不可用不会清除登录；同账号可在多个标签页浏览，但同一画布不能并发编辑。
- 后端账号媒体上传大小修复：后端 `spring.servlet.multipart.max-file-size` 和 `max-request-size` 改为可配置，默认 `512MB`，分别可通过 `USER_FILE_MAX_SIZE` 和 `USER_REQUEST_MAX_SIZE` 覆盖。需要确认：后端重启后，后端账号保存模式下上传图片、360 全景图、视频和音频到 `/api/user/files` 不再出现 413，刷新画布后媒体仍能通过 `backend:<id>` 文件 URL 正常恢复显示。
- 后端唯一账号工作区 + WebDAV 独立通道：正常业务不再提供“本地浏览器保存模式”。注册/登录后，配置、画布项目、素材、图片/视频/音频文件以及图片/视频生成记录分别通过 `/api/user/*` 保存到当前账号；未登录、工作区 bootstrap 未完成或后端不可用时，全局用户页面门禁会阻止进入空白工作区，且不会静默回退 localforage。首次账号加载只读取一次旧浏览器数据，完成媒体迁移和后端写入后清除旧业务缓存并记录已导入；WebDAV 继续作为用户显式选择的独立保存/同步通道。需要确认：错误注册码无法注册，正确注册码可注册并自动登录；刷新后保持账号和 `backend` 模式；用户 A/B 数据隔离；后端故障时显示工作区错误而不是空画布；旧数据只导入一次；WebDAV 测试连接、同步、远端合并和媒体下载不受影响。
- 后端生成记录与账号删除清理：图片和视频生成记录改由 `user_generation_logs` 按账号保存，图片/视频页面不再把 localforage 作为正常记录源；管理员删除账号时同步清理该账号的生成记录。需要确认：生成完成后刷新页面仍能看到记录；账号之间记录隔离；删除单条记录后不会恢复；管理员删除账号后对应生成记录被清理。
- 图片节点点击手势：带图片内容的图片节点现在应保持单击选中并可拖拽，双击打开节点下方 composer，三击打开图片查看；批量图片展开/收起仍通过右上角数量按钮操作，不再抢占图片双击。需要确认：普通图片节点、AI 生成图片节点、批量图片根节点和 360 场景节点都符合单击/双击/三击规则，三击不会触发 composer 残留遮挡，重复快速点击不触发 `Maximum update depth exceeded`。
- 360 全景沉浸式预览截图：360 场景节点三击后应打开黑底沉浸式 Three.js 全景预览，左键拖动旋转视角、滚轮缩放 FOV；点击“截图插入画布”后应把当前视角保存为普通图片节点，并自动从 360 节点连线到截图节点。需要确认：截图内容与当前视角一致，截图节点可继续用普通图片能力编辑/生成，关闭/重开预览没有 WebGL context 泄漏或控制台错误。
- 画布 360 场景节点：底部添加节点菜单新增“360场景”，点击后创建带内置 2:1 equirectangular panorama 提示词的图片节点；节点生成或上传全景图后，仅该节点使用 Three.js 球形内壁贴图预览，支持左键拖动旋转视角、滚轮缩放 FOV，并拦截节点内部拖拽/滚轮以避免误拖动画布或缩放画布；普通图片节点仍保持原 `<img>` 预览路径。需要确认：360 节点 composer 可输入并触发生成，Agnes/远程图片 URL 能通过代理转成 WebGL 可用贴图，本地上传的 2:1 全景图可正常查看，拖动/滚轮不会影响画布，普通文本/图片/视频/音频/脚本/导演台节点仍可正常点击、输入、拖拽和生成。
- StoryAI 3D 导演台替换：画布导演台已直接集成 `jiguang132/storyai-3d-director-desk`，不使用 iframe；旧导演台的 Three.js 场景、模型、骨骼、道具和资源释放模块已移除。每个导演台节点独立保存 StoryAI 项目，支持场景树、全骨骼角色、道具、全景背景、导演/机位视角、六方向视图、移动/旋转/缩放、相机属性与截图管理；“发送到画布”会上传截图、创建图片节点、连接导演台并在刷新后恢复。需要确认：多个导演台节点互不串数据，连续打开/关闭和刷新后项目保持，截图单张/批量回传正常，桌面与窄屏均可退出，控制台没有 `Maximum update depth exceeded`、WebGL context lost、纹理重复初始化或资源泄漏警告。
- LibTV 添加节点菜单补齐：画布底部“添加节点”菜单中的“脚本”会创建脚本节点，“视频合成”会创建专用视频合成节点并显示连接视频后的合成入口，“导演台”会创建导演台节点并显示“打开导演台”入口，“素材库”会打开资产管理的素材 Tab，“从生成历史选择”会打开图片/视频/音频分类的生成历史选择弹窗并可复制已有生成结果到画布。需要确认：十个入口（文本、图片、视频、视频合成、导演台、音频、脚本、素材库、上传、从生成历史选择）均可打开或创建正确对象，节点下方 composer 可输入并触发生成，生成历史弹窗分类和复制正常。
- LibTV 脚本/素材库/上传功能闭环：脚本节点现在会打开专用脚本工作台，可编辑标题、梗概、正文，预览自动分镜，并可一键创建分镜图片节点、旁白音频节点、脚本视频节点；素材库现在有风格库、效果库和我的素材三个入口，风格/效果预设可直接插入画布为提示词素材节点，我的素材支持文本、图片、视频、音频插入；上传支持图片、视频、音频以及 `.txt`、`.md`、`.markdown`、`.srt` 文本/脚本文件，脚本文件会创建脚本节点。需要确认：脚本三个动作生成的节点位置、连线和 composer 正常；素材预设插入后可继续生图/生视频；上传文本和脚本文件可正确创建/替换节点。
- AI 图片代理响应头与前端 fallback 修复：`/api/ai-proxy` 不再转发上游 HTTP/2 伪响应头（例如 `:status`）和 hop-by-hop 响应头，避免第三方 CDN 图片因浏览器 CORS fallback 到本地代理后，在 Vite dev proxy 层返回 500。前端 `image-storage.ts` 在同源 `/api/ai-proxy` 失败时会继续尝试已配置后端地址和本地开发后端端口，避免旧后端进程占用 `9801` 时图片无法回存。需要确认：Agnes 返回的 `platform-outputs.agnes-ai.space` 图片 URL 能被本地代理下载并存入画布图片节点，控制台不再出现 fallback 代理 500。
- LibTV 画布 UI/交互对齐：画布使用中性低重量背景和细网格；左下角提供“资产管理 / Mini Map / 重置 / 缩放百分比”组合；桌面创作工具栏固定在右侧并向左展开浮层，移动端保持底部横向工具栏，包含添加节点、工具箱、素材库、角色库、历史、快捷键、教程和外观入口；“添加节点”按创作类型提供文本、图片、视频、视频合成、导演台、音频、脚本、素材库、上传和从生成历史选择。节点和 Composer 沿用当前画布主题与紧凑创作界面。需要确认：桌面和移动端各工具栏入口均可打开/关闭，右侧浮层不超出视口，新增各类节点后 Composer 自动出现，单选已有节点可打开 Composer，输入生成、上传素材、打开素材库、外观切换、撤销/重做、删除/清空、缩放弹层、小地图开关和节点 hover 操作都正常。
- LibTV 文本 / 图片 / 视频节点交互：文本正文保持双击进入编辑，普通节点标题支持双击就地重命名；图片节点保留上传替换、360 场景、裁剪、蒙版、拆分、放大、多角度、预览、下载和保存素材等现有入口；视频节点在 hover 或选中时显示播放/暂停、进度、时间、静音、音量、全屏，以及“截取首帧 / 当前帧 / 尾帧”。截帧成功后应在视频右侧创建对应图片节点并自动连线。需要使用一条真实后端视频验证三种截帧、节点拖拽、双击 Composer 和三击预览互不冲突。
- Composer 生成按钮配置预检：当前模型渠道未填写 API Key 时，点击“生成”会显示明确提示并打开渠道配置，已输入的提示词不会再被清空；渠道配置完整时继续沿用原生成流程。需要确认图片、视频、音频、文本节点均符合该行为，ComfyUI 生成不受 AI 渠道 API Key 校验影响。
- 画布性能优化：`CanvasNode` 不再接收缩放 prop，缩放时节点本体不应因为 scale 变化整体重渲染；节点 resize 改为拖动中仅本地视觉预览、松手后提交一次尺寸；资源引用/生成输入改为复用连接索引，避免每个节点重复扫描全量连线；助手面板、图片编辑弹窗、素材选择、缩略图和节点面板改为按需加载；画布项目持久化拆成项目列表元数据与单项目详情分片。需要确认：缩放/拖拽/resize 手感正常，生成配置节点的上游引用标签正确，刷新后画布详情、会话、视图和项目列表仍能恢复，打开各类弹窗时按需加载无空白或报错。
- 画布低缩放真实节点可见性：缩放进入概览态后仍挂载并显示文本、图片、视频、音频、脚本、生成配置、导演台、360 全景、组等真实节点内容，不再替换成便签式或通用轻量占位壳；仅隐藏 hover 工具条、resize 手柄、连接手柄和媒体元数据徽标等交互层。需要在 100%、50%、25%、10% 分别确认节点类型仍可辨认、图片/视频/音频内容不消失、SVG 连线持续可见、缩放和平移无明显卡顿；桌面右侧工具栏浮层不越界，移动端底部工具栏可用，控制台无报错。
- 画布普通生成面板引用去重：在普通生成面板输入 `@` 并选择同一个上游文本节点一次时，只应保留一个 `文本1` 标签；已有的相邻重复标签应在面板重新打开后自动折叠为一个。
- ComfyUI 接入基础：配置弹窗新增 ComfyUI Tab，可配置本地或远端地址、连接方式、Client ID、任务超时和轮询间隔；新增顶部导航入口 `/comfyui`，可导入 ComfyUI API 格式 workflow JSON，选择节点输入作为暴露参数，编辑字段类型、默认值、范围和下拉选项，并可通过 Next.js 转发试运行 workflow。画布配置节点新增 ComfyUI 模式，可选择 workflow、填写暴露参数并生成输出图片、视频或音频节点（根据 ComfyUI 输出类型自动判定）。需要确认：本地 `http://127.0.0.1:8188` 和远端 ComfyUI 地址连接测试可用，导入 workflow 后参数保存/刷新仍存在，试运行和画布节点生成都能拿到 ComfyUI 输出图片/视频/音频。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- AI 请求代理：每个模型渠道新增“前端直连 / Next.js 转发”代理模式选择，开启后该渠道的文生图、图生图、视频、音频、文本问答和拉取模型列表会走 `/api/ai-proxy` 透传，用于绕过不开放 CORS 的第三方网关。默认仍为前端直连，需要单独确认每个代理渠道生图、生视频、拉模型是否正常。
- AI 返回的图片 / 视频 URL 在源头按渠道 useProxy 改写为 `/api/ai-proxy?target=...`，下游 fetch 拿到的就是同源 URL，避免第三方 CDN 报 CORS。`image-storage.ts` 仍保留 fetch 失败时自动 fallback 到代理作为防御。需要确认直连渠道的图片下载仍然走原 URL 不变。
- Agnes 视频生成：轮询任务改为请求 `/agnesapi`，不再被自动拼成 `/v1/agnesapi`；创建参数按官方文档传入 `model`、`prompt`、`image`、`width`、`height`、`num_frames` 和 `frame_rate`，并避免把非视频响应保存成黑屏本地视频。需要确认 Agnes 渠道创建任务、轮询结果和画布视频播放都正常。
- Agnes 双图首尾帧模式修复：后端能力校验将 `extra_body.mode=keyframes` 且包含两张图片的请求识别为 `first-last-frame`，三张及以上才识别为 `multi-frame`；前端同时显示后端返回的纯文本错误。已通过本地 `9801` 与固定 cpolar 地址创建双图任务并返回 200，需要在画布 Composer 中确认首尾帧生成、错误提示和后续轮询播放完整正常。
- Agnes 图生视频参考图：本地图片不再以 base64/data URL 直传给 Agnes，避免 `Invalid image: Incorrect padding`；会先上传到临时图床拿公网 URL，再作为 `image` 参数提交。需要确认本地上传图片、素材图片和公网图片 URL 三种参考图都能正常生成。
- 后端驱动的视频模型能力：新增匿名 `GET /api/model-capabilities/video`，Composer 的文生视频、图生视频、首尾帧、图片参考、全能参考，以及比例、清晰度、时长、声音、水印、样片和数量选项均由后端能力目录决定。Seedance 2.0 完整版提供 `480p/720p/1080p/4k`，Fast/Mini 仅提供 `480p/720p`；Seedance 1.5 Pro 开启样片模式时强制使用 `480p`。请求会按模式写入 `first_frame`、`last_frame`、`reference_image`、`reference_video`、`reference_audio`，并把支持的 `ratio`、`resolution`、`duration`、`generate_audio`、`watermark`、`draft` 作为顶层参数。需要使用真实 Agnes 和火山账号分别确认各模型可选项、任务创建、轮询、声音/水印和 1.5 Pro 样片模式；本次静态验证不调用付费生成接口。
- 即梦配置 Tab：配置弹窗新增"即梦"Tab，使用即梦原生 API（`visual.volcengineapi.com`），需填入 AK/SK 两个参数（非方舟 Bearer Token），请求经 `/api/jimeng-proxy` 服务端 HMAC-SHA256 签名转发。即梦模型使用 req_key 体系，与方舟渠道的 `doubao-*` 模型名完全独立。图片能力：文生图4.0（`jimeng_t2i_v40`）、文生图4.6（`jimeng_seedream46_cvtob`）、文生图3.0/3.1（`jimeng_t2i_v30`）、图生图3.0智能参考（`jimeng_i2i_v30`）、智能扩图（`jimeng_img2img_seed3_painting_edit`）、交互编辑/局部重绘（`jimeng_image2image_dream_inpaint`）、素材提取（`i2i_material_extraction`）、智能超清（`jimeng_i2i_seed3_tilesr_cvtob`）；视频能力：视频3.0Pro 1080P（`jimeng_ti2v_v30_pro`）、视频3.0 720P（`jimeng_t2v_v30`）。启用后在画布和视频工作台选择即梦能力即可生图/生视频。方舟渠道（Bearer Token）仍可并行使用。需要确认：AK/SK 填入 → 启用 → 选择即梦能力 → 生图/生视频正常。
- 后端配置能力：Java Spring Boot 后端（`backend/`）保留 SQLite 存储、`GET/PUT /api/config` 和 `GET /api/health` 端点，`/api/config` 继续通过 `AUTH_CODE` Bearer Token 认证作为兼容/管理接口；前端配置弹窗中的旧“后端同步”Tab 已移除，不再由用户在前端填写后端地址和认证码。需要确认：后端启动后健康检查可用、`AUTH_CODE` 校验仍对旧配置接口生效、账号保存模式使用 `/api/user/*` 同步用户自己的配置、Docker Compose 两个服务同时启动可用。
- 公网图片代理：后端新增 `POST /api/public-image`（需认证，上传图片）和 `GET /api/public-image/{filename}`（无需认证，公网可访问）端点。公网访问地址不再从旧“后端同步”前端页面配置，应由后端部署配置统一提供。Agnes 视频生成时，若后端可提供公网地址，本地参考图优先上传到后端获取公网 URL；未设置时降级到 temp.sh/litterbox 临时图床。需要确认：后端上传图片后可通过公网地址访问、Agnes 图生视频使用后端公网 URL 正常生成。
- 后端独立配置文件：后端启动时会自动读取 `backend/backend-config.yml`、`backend/data/backend-config.yml`，或从仓库根目录启动时读取 `backend/backend-config.yml`，其中 `app.public-base-url` 用于拼接公网图片访问地址，当前配置为固定地址 `https://gycmservice.nas.cpolar.cn`；`app.comfyui-base-url` 用于后端 ComfyUI 代理目标地址。修改该文件后需要重启后端生效。需要确认：上传到 `/api/public-image` 后返回的 `url` 使用固定的 cpolar 公网域名，ComfyUI 后端代理请求使用配置文件里的 ComfyUI 地址。
- ComfyUI 暴露参数与上游节点绑定：画布配置节点切换到 ComfyUI 模式时重新加载工作流列表，工作流无暴露参数时给出提示。image/video/audio 类型的暴露字段渲染为上游节点选择器（而非纯文本输入），只展示类型匹配的上游节点（图片字段只能选图片节点，视频字段只能选视频节点，音频字段只能选音频节点），选择后以 `@[node:nodeId]` 格式存储。生成时自动将引用的上游媒体上传到 ComfyUI `/upload/image`，用返回的文件名替换字段值并注入工作流 JSON。需要确认：暴露参数正确展示、类型匹配过滤生效、上游图片/视频/音频上传到 ComfyUI 后工作流执行正常。
- 局部蒙版 ComfyUI 消除：图片节点的局部遮罩编辑弹窗新增 ComfyUI 工作流选择和“ComfyUI 消除”入口。工作流同时暴露普通图片字段和名称包含 `mask` / “蒙版” / “遮罩”的图片字段时，原图与黑白透明蒙版会分别上传并注入；仅暴露一个普通图片字段时，画布会把选区写入原图 Alpha 通道，供 ComfyUI `LoadImage` 的 `MASK` 输出读取。需要确认两类 inpaint 工作流均能识别涂抹区域、输出图片节点并保留原节点连线；只暴露蒙版而未暴露原图时应显示明确提示。
- 后端账号配置同步：旧的前端手动“从后端拉取/推送到后端”按钮已随“后端同步”Tab 移除；后端账号模式下配置跟随登录用户通过 `/api/user/config` 自动同步。需要确认：换账号后配置不串号，刷新后当前账号配置可恢复，本地和 WebDAV 模式不受后端账号配置影响。
- 废弃后端同步地址清理：历史本地配置里若保存了 `1539e726.r23.cpolar.top`，启动恢复配置时会自动关闭并清空旧后端同步配置，避免继续请求已不使用的 cpolar 地址。由于旧“后端同步”Tab 已移除，后端地址不再通过前端页面重新填写。需要确认：刷新后控制台不再请求该域名，账号保存和 WebDAV 保存仍可正常使用。
- 画布上游引用图片预览修复：普通 `@` 引用下拉和配置编辑器引用预览在图片节点带 `storageKey` 时，会从本地持久化资源重新解析缩略图，不再使用刷新后失效的旧 `blob:` 地址作为兜底。生成节点读取上游图片时也会优先用 `storageKey` 还原本地图片，无法还原的失效图片引用会被跳过，避免 `Failed to fetch` 直接触发运行时错误。需要确认：刷新页面后，上游图片节点在引用下拉中能显示缩略图；带本地存储的上游图片可正常参与图生图/视频/文本问答；资源缺失时显示图标兜底或跳过失效引用，而不是坏图或 Runtime Error。
- ComfyUI 工作流后端同步（双文件模式）：后端工作流存储改为双文件分离模式——`xxx.json` 保持原始 ComfyUI API JSON 不修改，`xxx.config.json` 存放 title 和 fields 配置。API 端点调整为 `GET /api/workflows`（列表）、`GET /api/workflows/{id}`（详情）、`POST /api/workflows/upload`（上传原始工作流）、`PUT /api/workflows/{id}/config`（保存配置）、`DELETE /api/workflows/{id}`（删除）。前端导入工作流时自动上传原始 JSON 到后端，保存配置时只推送 config，同步时按 id+title+fields 合并。工作流 id 改为文件名（去 `.json`），确保本地和后端一致。需要确认：导入工作流后后端 `data/workflows/` 有原始 `.json` 和 `.config.json` 两个文件、原始文件内容不被修改、保存配置只更新 `.config.json`、换浏览器拉取能恢复工作流和参数配置。
- 画布定位、拖动与测试基础设施：节点搜索和分组定位使用正确的缩放后居中公式；生成记录兜底结算只在运行任务进入终态时触发，不再随每一帧节点拖动额外派发全量节点更新；连接锚点的全局指针监听会按 pointerId 正确结束并在卸载时清理。前端已接入 Vitest，原有测试已统一纳入 `pnpm test`，并新增视口定位和生成记录结算回归。需要确认：搜索定位不同缩放下均准确居中，包含大量节点和生成历史的画布拖动无额外卡顿，连接点单击创建和拖线互不误触。
- 画布 drag store 多画布串扰隐患：`useCanvasDragStore` 当前是模块级 zustand 单例，单页单画布下不串扰；但未来支持多 tab / 多个 `Canvas` 同时挂载 / 窗口分屏预览时，A 画布的拖动 offset 和 `draggedIds` 会跨画布叠加，导致节点跨画布错位。建议未来重构为 React Context Provider 形式（每个 `<CanvasProvider>` 持有独立 store 实例），或在 store 内加入 `scopeKey` 字段做画布隔离。当前规模不处理，但需在迁移到多画布时优先修复。需要确认：当前单画布下不会出错；启用多画布前先重构。
- 画布缩放与连线指引交互：低缩放概览模式下，图片、视频、音频节点继续显示真实媒体预览，不再退化成纯图标占位；只有 resize、连接手柄、composer、hover 工具栏等重交互控件会在概览态隐藏。选中单条连接线时，连接线本体会出现从上游节点流向下游节点的动态亮段和光点，用于指示数据/生成方向；普通相关高亮连接线保持静态。需要确认：包含图片/视频/音频的画布缩小到全局视图后媒体仍可辨认；点击连接线后动效方向与连接方向一致，取消选择后动效消失。
- 画布生成等待动效：文本、图片、视频、音频及批量图片根节点进入生成中状态时，内容区显示近黑底与宽幅柔和灰白扫光；扫光约 7 秒一轮，其中约 4 秒从左向右移动、约 3 秒停在右侧不可见区域，不再显示蓝色窄条或可见“生成中”文案。需要确认：真实生成期间动效持续流畅，成功后直接切换结果，失败后切换错误态；缩放、拖拽节点和打开 Composer 均不受影响。
- Leafer 画布缩放、组名与生成配置节点修复：移除高倍缩放下错误的固定平移边界，500% 时仍可用中键或空格拖拽向左右两侧移动；20% 等低倍缩放下节点保留反向缩放的类型/名称标签，图片等媒体内容仍可识别，低于 30% 只隐藏完整 hover 工具条；组不再显示内部节点数量，右上角显示可双击编辑的自定义组名；生成配置节点使用当前画布主题，生图/文本/视频/音频分别使用各自默认模型，ComfyUI 缺少工作流时可直接进入工作流页面，模型下拉和设置控件不会拖动节点；Composer 可输入并在一次空白点击后关闭。需要确认：长期编辑和连续切换模式后节点位置不漂移，500% 下左右平移均无边界锁死，低倍缩放标签不互相遮挡，自定义组名刷新后仍保留，配置完整时各模式真实生成链路可返回结果。
- 画布 Creative OS 视觉升级：画布采用内容优先的 Apple HIG 启发式视觉层级，统一系统字体、冷中性色、系统蓝焦点、8px 内容表面、克制阴影与 reduced-motion；顶部状态栏、浮动 Dock、缩放控件、节点、Composer、生成配置节点和快捷键面板使用一致的主题材质，并保留浅色/深色模式。需要确认：常用创作流程中工具按需出现且不遮挡内容，Composer 在窄屏和不同缩放下保持可读、可输入并可一次空白点击关闭，生成配置的模式/模型选择器可用，500% 缩放下可向左右双向平移，浅色和深色主题均无错位或低对比控件。
- 画布右键、动态组成员与节点悬停菜单修复：首次在空白画布右键不再因上下文菜单懒加载触发整页 fallback 黑闪；普通节点拖入已有组后会在拖拽结束时加入该组，拖出组范围后自动解除，之后拖动组时新增成员应一起移动；图片、文本、视频节点与上方悬停菜单之间增加延迟关闭和透明命中桥，鼠标从节点移向“360 场景 / 查看大图 / 编辑”等操作时菜单不会提前消失。需要确认：首次右键和连续右键均无闪屏，组内外拖拽及刷新后成员关系正确，快速和慢速移入菜单都能点击，控制台无 `Maximum update depth exceeded`。

- 画布滚轮与浏览器缩放隔离：画布外壳使用捕获阶段的非被动 `wheel` 监听器拦截 `Ctrl/Meta + 滚轮`，Leafer 画布自身在 Composer、工具栏等 `data-canvas-no-zoom` 区域也会取消浏览器级缩放；普通滚轮仍交给画布平移或 Composer 内部滚动。需要确认：空白画布、节点、Composer、顶部工具栏和浮层上使用触控板双指缩放或 `Ctrl/Meta + 滚轮` 时浏览器窗口比例不变化，普通滚轮仍能正常缩放/平移画布或滚动浮层。
- 后端账号工作区恢复：浏览器刷新或重新打开后先恢复账号、token 与 `backend` 模式，再通过 `/api/auth/me` 校验会话并调用 `/api/user/bootstrap`，以后端项目、素材和配置恢复运行态。仅在该账号从未完成旧数据导入时读取一次 legacy localforage，并按项目 ID/更新时间合入后端；导入成功后不再持续进行本地与后端双向合并。媒体迁移失败会使工作区保持错误态，不允许以缺失媒体的空白本地状态继续保存。需要确认：关闭并重新打开浏览器后自动恢复后端画布；不同域名登录同一账号可看到相同后端项目；后端临时不可用时页面锁定且登录不被清除；服务恢复后自动重试 bootstrap。
- 配置节点视频生成与播放修复：Agnes 完成响应只接受真正的 HTTP(S) 视频地址，不再把普通视频 ID 当作媒体 URL；第三方视频直连下载失败时自动回退到同源后端代理，验证返回内容确实为视频后再保存为稳定的本地或后端账号 `storageKey`。后端代理会转发 `Range` 并保留上游 `206 / Content-Range / Accept-Ranges`，视频节点使用元数据预加载且加载失败时显示明确错误态。需要确认：配置节点使用 Agnes、Seedance 或 OpenAI 兼容视频模型生成后能够显示和播放，刷新画布后视频仍存在，拖动进度条能够正常分段加载，失败响应不会生成空白成功节点。
- ComfyUI 视频输出加载：兼容 SaveVideo 等节点把 MP4 文件描述放在 `outputs.*.images` 或自定义字段中的情况，按文件扩展名、MIME/format 和旧字段提示依次识别图片、视频、音频，并去除重复输出。需要确认配置节点生成 MP4 后创建的是视频节点，后端账号工作区能上传并播放，刷新画布后媒体仍可恢复；WebDAV 显式通道保持原有媒体同步行为。
- ComfyUI 文本输出：工作流任务完成后会从 history 输出中的 `text`、`string`、`result`、`output` 等文本字段提取内容，并按照工作流输出节点顺序在 ComfyUI 节点右侧创建文本节点和连线；纯文本工作流不再被误报为“没有返回任何输出”。需要使用图片反推提示词工作流确认正向、负向、完整提示词分别生成文本节点，内容顺序正确，刷新画布后仍可编辑和引用。
- 生成结果刷新持久化：页面进入后台、刷新或关闭前会在后端同步监听器执行前，把最新节点、连线和节点顺序立即写入画布 store，避免 300ms 画布保存防抖尚未触发时丢失刚返回的图片。恢复画布时，带 `content` 或 `storageKey` 的图片、视频、音频节点即使旧状态仍为 `loading`，也会按已有媒体结果恢复为成功，不再显示“页面刷新后生成已中断”。需要确认图片刚显示后立即刷新、切换标签页、关闭再打开三种场景均能保留结果；真正未返回媒体的中断节点仍应显示重试提示。
- 全节点生成任务刷新续跑：文本、图片、视频、音频和 ComfyUI 节点在请求模型前会把用户级 `generationJobId` 强制保存到账号画布；后端模型代理与 ComfyUI 排队接口按该 ID 持久化并回放原始响应。刷新或关闭页面不会取消后端执行，重新打开画布后会自动恢复仍为 `loading` 的任务；视频在厂商任务 ID 尚未返回时也能恢复创建响应，返回后继续沿用原有厂商轮询；批量图片和批量文本为每个输出节点保存独立任务 ID。生成请求统一以 30 分钟为客户端和后端上限，页面关闭不再弹出“生成将中断”的离开确认。需要分别确认文生图、图生图、文本、多模态文本、音频、Seedance/Agnes/OpenAI 视频、ComfyUI 图片/视频/音频/文本输出在“刚提交即刷新”“执行中关闭后重开”“结果返回瞬间刷新”三种场景均不会重复提交或丢失结果，并确认不同账号不能复用彼此的任务响应。
- ComfyUI 公网代理稳定性：历史轮询遇到临时网络错误或 502/503/504 时会有限退避重试，不再因隧道瞬时抖动直接判定生成失败；视频 `/view` 改为流式转发并保留上游状态码、`Range`、`Content-Range`、`Accept-Ranges` 和媒体类型，错误 JSON 或空响应不会再被保存成视频文件。需要确认通过固定公网地址生成较长 MP4 时任务能完成并插入视频节点，播放、拖动进度条和刷新恢复均正常，持续故障仍能给出明确错误。
- 后端账号同步瞬时错误恢复：首次工作区加载不再重复推送配置和素材；配置、画布、素材的幂等 GET/PUT 请求遇到网络中断或 502/503/504 时会短退避重试两次，持续故障则由共享的 5 秒延迟补偿继续同步，不再立即递归形成请求风暴。媒体上传 POST 不自动重放，避免重复文件。需要确认：通过固定 cpolar 地址登录后刷新画布，短暂断网或隧道抖动恢复后配置、画布和素材会自动补同步，控制台不再连续刷出 initial config push failed。
- 视频节点拖拽与原生控制条交互：视频画面主体已从 Leafer DOM 交互白名单中移除，保持为画布节点拖拽区域，浏览器原生媒体拖影已禁用；底部播放控制区仍通过按钮和输入控件独立接收播放/暂停、进度、音量和全屏交互，不触发节点拖动。自动回归已确认从视频内容区拖动时节点位置实时更新。需要继续确认：已加载视频播放中仍可从上部或中部拖动并保存位置，底部控制条全部可用，图片、音频、文本节点的拖拽与点击行为不受影响。
- 重开画布后首次创建节点与自动保存恢复：后端账号工作区已经完成首次 bootstrap 后，配置、画布或素材自动保存的瞬时网络错误不会再把 `workspaceStatus` 切换为会卸载画布的错误页，当前内存编辑会保留并由 5 秒延迟补偿重试；异步媒体恢复按账号、保存模式和项目 ID 只执行一次，过期恢复结果不会覆盖用户刚创建的节点。已验证重新打开画布后第一次右键添加文本节点立即成功，等待自动保存后节点仍在，再次刷新可从后端恢复。需要继续确认：首次创建图片、视频、音频、脚本和配置节点同样不会触发画布闪烁或丢失；模拟一次 502/网络中断后未保存节点仍留在画布，服务恢复后自动同步成功。
- LeaferJS 画布帧率优化：移除重复的 LeaferJS 点阵/网格对象，滚轮缩放、滚轮平移和鼠标拖动画布改为先直接更新 DOM/Leafer 展示，再在滚轮停止 100ms 或拖拽结束时提交 React 视口状态；Composer 与临时 SVG 连接线在过渡期间同步跟随。自动回归已覆盖 Composer 打开、输入、生成按钮命中、单击空白关闭、节点拖拽、连接线 hover、临时拉线与空白创建菜单，未出现 `Maximum update depth exceeded`。需要继续人工确认：大型真实画布连续缩放/平移保持流畅，Composer、图片/视频/音频媒体、组、360 场景、导演台和后端自动保存均无视觉跳动或状态丢失。
- 画布媒体视口缓存：图片、视频、音频节点首次进入视口并完成媒体绑定后会继续保持挂载，平移离开再返回或跨越概览缩放阈值时不再重复创建媒体元素；后端账号媒体响应增加私有长期缓存。需要确认大型画布中未访问节点仍按视口裁剪，已访问媒体无空白闪烁、视频/音频播放状态不重置、Network 不重复请求同一文件，并验证删除媒体节点后不会残留。
- 独立模型管理后台与验证发布门禁：创作端设置和导航不再展示 `/admin` 入口，旧 `/comfyui` 也不再跳转后台；管理员只能直接访问独立 `/admin` 页面，并使用管理员账号、密码和管理员授权码登录。模型接入按“厂商认证并拉取 -> 分类与能力配置 -> 后端验证真实模型 ID -> 管理员显式发布”执行。后台不要求填写官方文档 URL 或勾选无法被系统核实的人工复核项；验证不会触发付费生成。客户端保存不能伪造验证状态，厂商凭证、模型 ID、适配器或能力参数变化会自动撤销验证和发布；运行时目录及图片/视频能力接口只返回已验证且已发布模型。需要确认：创作端源码和 UI 没有后台入口；普通账号不能访问管理接口；错误厂商凭证验证失败；正确凭证能拉取并验证真实模型；验证成功后仍需手动发布；修改已验证模型后立即回到待验证且从画布消失；API Key、厂商真实地址和 `requestModel` 不进入创作端运行时响应。

- 生产就绪回归收口：真实浏览器已验证后端账号画布自动保存与刷新恢复、低缩放节点可见、SVG 连线创建/吸附/撤销、缩放和平移、节点拖动、图片 Composer 双击打开与单击空白关闭、文本编辑、多选成组、首次右键创建菜单及图片三击进入 360 预览；最终验证窗口未出现新的 `Maximum update depth exceeded`。前端 TypeScript、后端 7 个 Maven 测试及 `git diff --check` 均通过。仍需使用已配置的真实厂商账号人工执行一次图片/视频生成，确认付费任务创建、轮询、产物上传、刷新恢复和播放全链路；若旧画布引用的历史媒体 Blob 已不存在，该媒体本身无法自动恢复，但不会再阻塞其他画布自动保存。
