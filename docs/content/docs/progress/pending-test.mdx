---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- 后端账号保存 + WebDAV 云端保存保留：配置弹窗新增“保存位置”，可在“本地浏览器 / 本地后端账号 / 云端 WebDAV”之间切换；后端模式需要注册/登录后使用，注册时必须填写后端配置中的 `AUTH_CODE` 鉴权码，登录后配置、画布项目、素材和图片/视频/音频文件会通过 `/api/user/*` 保存到当前账号；旧“后端同步”配置页已移除，前端不再让用户填写后端地址和旧认证码，后端地址由部署/后端配置统一决定；首次进入后端模式可一键导入本地数据且不删除本地数据；WebDAV Tab 保留为“云端 WebDAV 保存/同步”，继续使用原有 WebDAV 地址、用户名和密码同步 canvas/assets/logs/files/config。需要确认：错鉴权码无法注册，正确鉴权码可注册并自动登录；登录、刷新保持登录态、退出后后端用户接口返回未授权；用户 A/B 只能看到自己的画布、素材、配置和 `backend:<id>` 文件；本地模式仍沿用 localforage；WebDAV 测试连接、同步、远端合并和媒体下载不受后端账号影响；后端模式上传媒体后刷新画布仍能通过后端文件 URL 恢复显示；AI 渠道/API Key/ComfyUI 配置在三种保存通道下不会串账号或串通道。
- 图片节点点击手势：带图片内容的图片节点现在应保持单击选中并可拖拽，双击打开节点下方 composer，三击打开图片查看；批量图片展开/收起仍通过右上角数量按钮操作，不再抢占图片双击。需要确认：普通图片节点、AI 生成图片节点、批量图片根节点和 360 场景节点都符合单击/双击/三击规则，三击不会触发 composer 残留遮挡，重复快速点击不触发 `Maximum update depth exceeded`。
- 360 全景沉浸式预览截图：360 场景节点三击后应打开黑底沉浸式 Three.js 全景预览，左键拖动旋转视角、滚轮缩放 FOV；点击“截图插入画布”后应把当前视角保存为普通图片节点，并自动从 360 节点连线到截图节点。需要确认：截图内容与当前视角一致，截图节点可继续用普通图片能力编辑/生成，关闭/重开预览没有 WebGL context 泄漏或控制台错误。
- 画布 360 场景节点：底部添加节点菜单新增“360场景”，点击后创建带内置 2:1 equirectangular panorama 提示词的图片节点；节点生成或上传全景图后，仅该节点使用 Three.js 球形内壁贴图预览，支持左键拖动旋转视角、滚轮缩放 FOV，并拦截节点内部拖拽/滚轮以避免误拖动画布或缩放画布；普通图片节点仍保持原 `<img>` 预览路径。需要确认：360 节点 composer 可输入并触发生成，Agnes/远程图片 URL 能通过代理转成 WebGL 可用贴图，本地上传的 2:1 全景图可正常查看，拖动/滚轮不会影响画布，普通文本/图片/视频/音频/脚本/导演台节点仍可正常点击、输入、拖拽和生成。
- LibTV 导演台全屏工作区对齐：导演台节点的“打开导演台”现在打开全屏 `3D导演台`，默认右侧为 `3D场景` 面板，包含场景缩放、平移、旋转、全景背景、全景球、角色标签、网格吸附和地面控制；新建导演台默认包含 `机位1`、`机位2`、`角色A`；点击机位后切换到 `摄像机` 面板并显示 `属性 / 摄像机截图` Tab；底部 `截图` 会写入截图列表，`发送到画布` 会创建与导演台相连的图片节点。需要确认：多次打开/关闭导演台、添加机位、截图、全部清空、发送到画布、刷新后恢复状态都符合预期。
- 导演台具体功能补实：`3D场景` 面板中的缩放、平移、旋转、天空色、全景球、角色标签、网格吸附、地面透明度/高度会写入节点配置并影响中间舞台；`角色` 面板可编辑角色名、颜色、位置、显示和锁定；`摄像机` 面板可编辑机位名、镜头说明、位置、注视目标、注视坐标、FOV、截图提示词、显示和锁定；截图预览和发送到画布后的图片节点 prompt 会带上这些具体参数。需要确认：修改参数后舞台即时变化，截图缩略图随参数变化，刷新后参数恢复，发送到画布的图片节点标题、连线和 prompt 正常。
- 导演台 Three.js 舞台：导演台中心区域已从 CSS 假 3D 替换为 Three.js/WebGL 渲染，包含真实 `WebGLRenderer`、透视相机、灯光、地面、网格、角色 mesh、机位相机体和视锥辅助线；`导演视角 / 机位视角` 会切换渲染相机。需要确认：打开导演台后 WebGL 画面非空，调天空色/地面/角色/机位/FOV 时 3D 画面即时变化，切到机位视角后视角正确，关闭/重开没有 WebGL context 泄漏或控制台错误。
- LibTV 添加节点菜单补齐：画布底部“添加节点”菜单中的“脚本”会创建脚本节点，“视频合成”会创建专用视频合成节点并显示连接视频后的合成入口，“导演台”会创建导演台节点并显示“打开导演台”入口，“素材库”会打开资产管理的素材 Tab，“从生成历史选择”会打开图片/视频/音频分类的生成历史选择弹窗并可复制已有生成结果到画布。需要确认：十个入口（文本、图片、视频、视频合成、导演台、音频、脚本、素材库、上传、从生成历史选择）均可打开或创建正确对象，节点下方 composer 可输入并触发生成，生成历史弹窗分类和复制正常。
- LibTV 脚本/素材库/上传功能闭环：脚本节点现在会打开专用脚本工作台，可编辑标题、梗概、正文，预览自动分镜，并可一键创建分镜图片节点、旁白音频节点、脚本视频节点；素材库现在有风格库、效果库和我的素材三个入口，风格/效果预设可直接插入画布为提示词素材节点，我的素材支持文本、图片、视频、音频插入；上传支持图片、视频、音频以及 `.txt`、`.md`、`.markdown`、`.srt` 文本/脚本文件，脚本文件会创建脚本节点。需要确认：脚本三个动作生成的节点位置、连线和 composer 正常；素材预设插入后可继续生图/生视频；上传文本和脚本文件可正确创建/替换节点。
- 导演台可用工作台：点击导演台节点里的“打开导演台”会打开场景工作台，可编辑场景空间、角色、道具、视觉风格和机位截图列表，支持新增/删除机位，并通过“生成多视角截图”在画布右侧生成与导演台连接的多张分镜图片节点。需要确认：导演台面板字段可编辑，机位列表增删正常，生成后的图片节点位置、连线、标题和提示词正常，刷新后导演台配置和生成结果仍能恢复。
- AI 图片代理响应头与前端 fallback 修复：`/api/ai-proxy` 不再转发上游 HTTP/2 伪响应头（例如 `:status`）和 hop-by-hop 响应头，避免第三方 CDN 图片因浏览器 CORS fallback 到本地代理后，在 Vite dev proxy 层返回 500。前端 `image-storage.ts` 在同源 `/api/ai-proxy` 失败时会继续尝试已配置后端地址和本地开发后端端口，避免旧后端进程占用 `9801` 时图片无法回存。需要确认：Agnes 返回的 `platform-outputs.agnes-ai.space` 图片 URL 能被本地代理下载并存入画布图片节点，控制台不再出现 fallback 代理 500。
- LibTV 画布 UI/交互对齐：画布深色主题改为更接近 LibTV 的中性黑背景和低可见度细网格；左下角改为“资产管理 / Mini Map / 重置 / 缩放百分比”组合，小地图默认打开且尺寸更紧凑；底部工具栏改为 LibTV 式居中小 dock，包含添加节点、工具箱、素材库、角色库、历史、快捷键、教程和外观入口；“添加节点”弹层按 LibTV 分组提供文本、图片、视频、视频合成、导演台、音频、脚本、素材库、上传和从生成历史选择；快捷键改为贴底四栏大浮层，教程改为 dock 上方小菜单；工具箱、素材库、角色库、历史也都有对应深色浮层；左侧资产管理面板按“画布 / 资产”分 tab，并会与 dock 弹层互斥；新建文本、图片、视频、音频和生成配置节点后都会打开节点下方 composer，单选已有节点也会打开 composer；普通生成 composer 加宽并改成低矮横向输入面板；新增空文本/图片/视频/音频节点默认尺寸改为更紧凑的 LibTV 卡片尺寸；节点显示顶部小标题，空文本/图片节点显示 LibTV 式“尝试”快捷入口；节点 hover 操作条、节点圆角、描边、阴影和空状态更克制。需要确认：各底部入口可打开/关闭，新增各类节点后 composer 自动出现，单选节点 composer 自动出现，重复点击节点 composer 不消失，输入生成、上传素材、打开素材库、外观切换、撤销/重做、删除/清空、缩放弹层、小地图开关和节点 hover 操作都正常。
- 画布性能优化：`CanvasNode` 不再接收缩放 prop，缩放时节点本体不应因为 scale 变化整体重渲染；节点 resize 改为拖动中仅本地视觉预览、松手后提交一次尺寸；资源引用/生成输入改为复用连接索引，避免每个节点重复扫描全量连线；助手面板、图片编辑弹窗、素材选择、缩略图和节点面板改为按需加载；画布项目持久化拆成项目列表元数据与单项目详情分片。需要确认：缩放/拖拽/resize 手感正常，生成配置节点的上游引用标签正确，刷新后画布详情、会话、视图和项目列表仍能恢复，打开各类弹窗时按需加载无空白或报错。
- 画布百节点概览性能修复：低缩放概览态下，非选中节点改为轻量壳渲染，不挂载图片、视频、滚动文本、resize 手柄和连接手柄；缩放/平移视口更新合并到每帧一次；普通引用数据不再随视口变化重算；低缩放网格改为分级粗网格，减少密集背景绘制。需要确认：一百多个节点缩小到全局视图后拖拽、滚轮缩放、移动鼠标不再明显卡顿；选中节点、打开节点面板或放大后仍恢复完整内容和交互。
- 画布普通生成面板引用去重：在普通生成面板输入 `@` 并选择同一个上游文本节点一次时，只应保留一个 `文本1` 标签；已有的相邻重复标签应在面板重新打开后自动折叠为一个。
- ComfyUI 接入基础：配置弹窗新增 ComfyUI Tab，可配置本地或远端地址、连接方式、Client ID、任务超时和轮询间隔；新增顶部导航入口 `/comfyui`，可导入 ComfyUI API 格式 workflow JSON，选择节点输入作为暴露参数，编辑字段类型、默认值、范围和下拉选项，并可通过 Next.js 转发试运行 workflow。画布配置节点新增 ComfyUI 模式，可选择 workflow、填写暴露参数并生成输出图片、视频或音频节点（根据 ComfyUI 输出类型自动判定）。需要确认：本地 `http://127.0.0.1:8188` 和远端 ComfyUI 地址连接测试可用，导入 workflow 后参数保存/刷新仍存在，试运行和画布节点生成都能拿到 ComfyUI 输出图片/视频/音频。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- AI 请求代理：每个模型渠道新增“前端直连 / Next.js 转发”代理模式选择，开启后该渠道的文生图、图生图、视频、音频、文本问答和拉取模型列表会走 `/api/ai-proxy` 透传，用于绕过不开放 CORS 的第三方网关。默认仍为前端直连，需要单独确认每个代理渠道生图、生视频、拉模型是否正常。
- AI 返回的图片 / 视频 URL 在源头按渠道 useProxy 改写为 `/api/ai-proxy?target=...`，下游 fetch 拿到的就是同源 URL，避免第三方 CDN 报 CORS。`image-storage.ts` 仍保留 fetch 失败时自动 fallback 到代理作为防御。需要确认直连渠道的图片下载仍然走原 URL 不变。
- Agnes 视频生成：轮询任务改为请求 `/agnesapi`，不再被自动拼成 `/v1/agnesapi`；创建参数按官方文档传入 `model`、`prompt`、`image`、`width`、`height`、`num_frames` 和 `frame_rate`，并避免把非视频响应保存成黑屏本地视频。需要确认 Agnes 渠道创建任务、轮询结果和画布视频播放都正常。
- Agnes 图生视频参考图：本地图片不再以 base64/data URL 直传给 Agnes，避免 `Invalid image: Incorrect padding`；会先上传到临时图床拿公网 URL，再作为 `image` 参数提交。需要确认本地上传图片、素材图片和公网图片 URL 三种参考图都能正常生成。
- Seedance 视频 400 修复：`createSeedanceTask()` 不再向方舟 `/contents/generations/tasks` 端点发送 `resolution` 顶层字段（方舟不支持）；1.0/lite 模型改为在 content text 里用 `--resolution --duration --watermark --camerafixed` 格式传参，1.5+ 模型用顶层 `ratio`/`duration`/`watermark`/`generate_audio` 字段。需要确认 `doubao-seedance-1-0-pro-fast-251015` 和 `doubao-seedance-1-5-pro-251215` 都能正常创建任务。
- 即梦配置 Tab：配置弹窗新增"即梦"Tab，使用即梦原生 API（`visual.volcengineapi.com`），需填入 AK/SK 两个参数（非方舟 Bearer Token），请求经 `/api/jimeng-proxy` 服务端 HMAC-SHA256 签名转发。即梦模型使用 req_key 体系，与方舟渠道的 `doubao-*` 模型名完全独立。图片能力：文生图4.0（`jimeng_t2i_v40`）、文生图4.6（`jimeng_seedream46_cvtob`）、文生图3.0/3.1（`jimeng_t2i_v30`）、图生图3.0智能参考（`jimeng_i2i_v30`）、智能扩图（`jimeng_img2img_seed3_painting_edit`）、交互编辑/局部重绘（`jimeng_image2image_dream_inpaint`）、素材提取（`i2i_material_extraction`）、智能超清（`jimeng_i2i_seed3_tilesr_cvtob`）；视频能力：视频3.0Pro 1080P（`jimeng_ti2v_v30_pro`）、视频3.0 720P（`jimeng_t2v_v30`）。启用后在画布和视频工作台选择即梦能力即可生图/生视频。方舟渠道（Bearer Token）仍可并行使用。需要确认：AK/SK 填入 → 启用 → 选择即梦能力 → 生图/生视频正常。
- 后端配置能力：Java Spring Boot 后端（`backend/`）保留 SQLite 存储、`GET/PUT /api/config` 和 `GET /api/health` 端点，`/api/config` 继续通过 `AUTH_CODE` Bearer Token 认证作为兼容/管理接口；前端配置弹窗中的旧“后端同步”Tab 已移除，不再由用户在前端填写后端地址和认证码。需要确认：后端启动后健康检查可用、`AUTH_CODE` 校验仍对旧配置接口生效、账号保存模式使用 `/api/user/*` 同步用户自己的配置、Docker Compose 两个服务同时启动可用。
- 公网图片代理：后端新增 `POST /api/public-image`（需认证，上传图片）和 `GET /api/public-image/{filename}`（无需认证，公网可访问）端点。公网访问地址不再从旧“后端同步”前端页面配置，应由后端部署配置统一提供。Agnes 视频生成时，若后端可提供公网地址，本地参考图优先上传到后端获取公网 URL；未设置时降级到 temp.sh/litterbox 临时图床。需要确认：后端上传图片后可通过公网地址访问、Agnes 图生视频使用后端公网 URL 正常生成。
- 后端独立配置文件：后端启动时会自动读取 `backend/backend-config.yml`、`backend/data/backend-config.yml`，或从仓库根目录启动时读取 `backend/backend-config.yml`，其中 `app.public-base-url` 用于拼接公网图片访问地址，当前配置为 `https://718d637e.r23.cpolar.top`；`app.comfyui-base-url` 用于后端 ComfyUI 代理目标地址。修改该文件后需要重启后端生效。需要确认：上传到 `/api/public-image` 后返回的 `url` 使用 cpolar 公网域名，ComfyUI 后端代理请求使用配置文件里的 ComfyUI 地址。
- ComfyUI 暴露参数与上游节点绑定：画布配置节点切换到 ComfyUI 模式时重新加载工作流列表，工作流无暴露参数时给出提示。image/video/audio 类型的暴露字段渲染为上游节点选择器（而非纯文本输入），只展示类型匹配的上游节点（图片字段只能选图片节点，视频字段只能选视频节点，音频字段只能选音频节点），选择后以 `@[node:nodeId]` 格式存储。生成时自动将引用的上游媒体上传到 ComfyUI `/upload/image`，用返回的文件名替换字段值并注入工作流 JSON。需要确认：暴露参数正确展示、类型匹配过滤生效、上游图片/视频/音频上传到 ComfyUI 后工作流执行正常。
- 后端账号配置同步：旧的前端手动“从后端拉取/推送到后端”按钮已随“后端同步”Tab 移除；后端账号模式下配置跟随登录用户通过 `/api/user/config` 自动同步。需要确认：换账号后配置不串号，刷新后当前账号配置可恢复，本地和 WebDAV 模式不受后端账号配置影响。
- 废弃后端同步地址清理：历史本地配置里若保存了 `1539e726.r23.cpolar.top`，启动恢复配置时会自动关闭并清空旧后端同步配置，避免继续请求已不使用的 cpolar 地址。由于旧“后端同步”Tab 已移除，后端地址不再通过前端页面重新填写。需要确认：刷新后控制台不再请求该域名，账号保存和 WebDAV 保存仍可正常使用。
- 画布上游引用图片预览修复：普通 `@` 引用下拉和配置编辑器引用预览在图片节点带 `storageKey` 时，会从本地持久化资源重新解析缩略图，不再使用刷新后失效的旧 `blob:` 地址作为兜底。生成节点读取上游图片时也会优先用 `storageKey` 还原本地图片，无法还原的失效图片引用会被跳过，避免 `Failed to fetch` 直接触发运行时错误。需要确认：刷新页面后，上游图片节点在引用下拉中能显示缩略图；带本地存储的上游图片可正常参与图生图/视频/文本问答；资源缺失时显示图标兜底或跳过失效引用，而不是坏图或 Runtime Error。
- ComfyUI 工作流后端同步（双文件模式）：后端工作流存储改为双文件分离模式——`xxx.json` 保持原始 ComfyUI API JSON 不修改，`xxx.config.json` 存放 title 和 fields 配置。API 端点调整为 `GET /api/workflows`（列表）、`GET /api/workflows/{id}`（详情）、`POST /api/workflows/upload`（上传原始工作流）、`PUT /api/workflows/{id}/config`（保存配置）、`DELETE /api/workflows/{id}`（删除）。前端导入工作流时自动上传原始 JSON 到后端，保存配置时只推送 config，同步时按 id+title+fields 合并。工作流 id 改为文件名（去 `.json`），确保本地和后端一致。需要确认：导入工作流后后端 `data/workflows/` 有原始 `.json` 和 `.config.json` 两个文件、原始文件内容不被修改、保存配置只更新 `.config.json`、换浏览器拉取能恢复工作流和参数配置。
- 画布前端基础设施缺口：仓库当前没有前端单元测试框架（`web/src/` 下零 `*.test.ts`）、没有 ESLint 配置（无 `.eslintrc*`）、`package.json` 没有 `lint` / `typecheck` / `test` 脚本，唯一静态检查是 prettier。本次画布性能改动（`e68f52d` + 工作区）依赖人工手测 React DevTools Profiler 验证 re-render 边界。建议补：(1) Vitest（覆盖 `canvas-client-page.tsx` 中的 `visibleNodes` / `visibleConnections` / `applyCanvasAgentOps` 等纯函数）；(2) ESLint（启用 react-hooks / `@typescript-eslint/recommended`）；(3) 在 `package.json` 增加 `lint`、`typecheck`、`test` 脚本与 CI 钩子。需要确认：补齐后 `npm run test / lint / typecheck` 均有有效输出，并能跑过本次改动的回归场景。
- 画布 drag store 多画布串扰隐患：`useCanvasDragStore` 当前是模块级 zustand 单例，单页单画布下不串扰；但未来支持多 tab / 多个 `Canvas` 同时挂载 / 窗口分屏预览时，A 画布的拖动 offset 和 `draggedIds` 会跨画布叠加，导致节点跨画布错位。建议未来重构为 React Context Provider 形式（每个 `<CanvasProvider>` 持有独立 store 实例），或在 store 内加入 `scopeKey` 字段做画布隔离。当前规模不处理，但需在迁移到多画布时优先修复。需要确认：当前单画布下不会出错；启用多画布前先重构。
- 画布缩放与连线指引交互：低缩放概览模式下，图片、视频、音频节点继续显示真实媒体预览，不再退化成纯图标占位；只有 resize、连接手柄、composer、hover 工具栏等重交互控件会在概览态隐藏。选中单条连接线时，连接线本体会出现从上游节点流向下游节点的动态亮段和光点，用于指示数据/生成方向；普通相关高亮连接线保持静态。需要确认：包含图片/视频/音频的画布缩小到全局视图后媒体仍可辨认；点击连接线后动效方向与连接方向一致，取消选择后动效消失。
